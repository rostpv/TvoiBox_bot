# Этапы разработки

Этот файл теперь ведется как рабочий журнал разработки в одном формате:

1. Этап.
2. Чек-лист текущего прогресса по этапу.
3. Логи по этапу (что сделано, трудности, решения).

## Текущий этап

- Текущий этап: `Этап 27. Автодеплой mini app из GitHub`
- Текущий статус: `Этап 26 завершен: внешний dev-контур mini app на VPS поднят, внешние client/trainer preview работают, безопасная Telegram-точка входа для ручной проверки зафиксирована. Следующий фокус — повторяемый dev-autodeploy mini app из GitHub.`

---

## Этап 1. Выбор стека и базовой структуры проекта

Статус: `Завершен`

### Чек-лист текущего прогресса по этапу

- [x] Зафиксирован стек проекта.
- [x] Зафиксирована структура monorepo.
- [x] Сверены ТЗ, стек и структура между собой.

### Логи по этапу

- Выполнено: утвержден стек `Node.js + TypeScript + NestJS + grammY + PostgreSQL + Prisma + Docker Compose + Caddy`.
- Выполнено: утверждена структура `apps / packages / infra / docs / scripts / logs`.
- Решение: `mini-app` оставлен в архитектуре, но вынесен за рамки MVP.

---

## Этап 2. Подготовка проекта и локального запуска

Статус: `Завершен`

### Чек-лист текущего прогресса по этапу

- [x] Создан каркас monorepo.
- [x] Настроены `api` и `bot`.
- [x] Подготовлены `.env` и `.env.example`.
- [x] Добавлено базовое логирование.
- [x] Пройден self-check локального старта.

### Логи по этапу

- Выполнено: созданы рабочие скрипты запуска и проверки (`typecheck`, `build`, `dev`).
- Выполнено: старт `api` и `bot` подтвержден по runtime-логам.
- Трудности: в окружении не было `pnpm`; запуск переведен на `corepack`.

---

## Этап 3. Проектирование и запуск базы данных

Статус: `Завершен`

### Чек-лист текущего прогресса по этапу

- [x] Подготовлена Prisma-схема под сущности MVP.
- [x] Подключен Prisma в `apps/api`.
- [x] Добавлены базовые проверки БД (`/health`, seed/read).
- [x] Добавлен `corepack pnpm db:runtime-check`.
- [x] Поднят живой локальный PostgreSQL.
- [x] Пройден успешный runtime-check подключения к БД.

### Логи по этапу

- Выполнено: создан модуль Prisma и сервисы проверки/seed для технической валидации слоя данных.
- Выполнено: добавлены команды `prisma:validate`, `prisma:generate`, `prisma:db:push`, `prisma:migrate:dev`.
- Выполнено: в текущем окружении поднят portable PostgreSQL (`.tools/postgres`) и создана БД `tvoy_box`.
- Self-check: `corepack pnpm db:runtime-check` завершился `Prisma runtime-check: OK`.

---

## Этап 4. Базовый каркас Telegram-бота

Статус: `Завершен`

### Чек-лист текущего прогресса по этапу

- [x] Реализована команда `/start`.
- [x] Реализовано главное меню.
- [x] Разделены роли `client/admin` по Telegram ID.
- [x] Добавлена навигация назад.
- [x] Добавлена базовая обработка ошибок.
- [x] Добавлены runtime-логи по пользовательским действиям.
- [x] Выполнена живая проверка запуска и `/start` в Telegram.

### Логи по этапу

- Выполнено: добавлены отдельные `handlers`, `menus`, `services` для каркаса бота.
- Выполнено: добавлена защита админских разделов от не-админа.
- Выполнено: по логам подтвержден живой запуск polling и обработка `/start`.

---

## Этап 5. Регистрация клиента и согласие

Статус: `Завершен`

### Чек-лист текущего прогресса по этапу

- [x] Реализован сценарий первого входа в боте.
- [x] Реализован шаг ввода имени.
- [x] Реализован шаг телефона с возможностью пропуска.
- [x] Реализован шаг согласия на обработку ПД.
- [x] Реализовано сохранение клиента через API.
- [x] Реализована проверка повторного входа без навязывания регистрации.
  - [x] При повторном `/start` профиль клиента подгружается автоматически из БД, регистрация не запускается повторно.
- [x] Прогнаны живые сценарии регистрации в Telegram с поднятыми `api + bot + db`.
  - [x] Перевести бота в live-режим (`BOT_DRY_RUN=false`) для ручного Telegram-прогона.
  - [x] Проверить исходящий доступ окружения к `api.telegram.org`.
  - [x] Восстановить исходящий TCP-доступ к `api.telegram.org:443`.
  - [x] Убедиться, что токен бота и `ADMIN_TELEGRAM_ID` заданы корректно в `.env`.
  - [x] Регистрация с телефоном.
  - [x] Регистрация без телефона.
  - [x] Отказ от согласия (блокировка продолжения).
  - [x] Повторный `/start` уже зарегистрированного клиента.

### Логи по этапу

- Выполнено: добавлен API-модуль клиентов (`GET /clients/telegram/:telegramId`, `POST /clients/register`) в `apps/api`.
- Выполнено: добавлен onboarding регистрации в `apps/bot` (имя -> телефон/пропуск -> согласие -> сохранение).
- Выполнено: без согласия пользователь не может пройти дальше по сценарию.
- Self-check: успешно выполнены `corepack pnpm typecheck`, `corepack pnpm --filter @tvoy-box/api build`, `corepack pnpm --filter @tvoy-box/bot build`.
- Выполнено (2026-05-12): проведена повторная ревизия текущего кода и структуры проекта (ТЗ, стек, структура, этапы, модули `api/bot` по регистрации).
- Self-check (2026-05-12): повторно успешно выполнены `corepack pnpm typecheck`, `corepack pnpm --filter @tvoy-box/api build`, `corepack pnpm --filter @tvoy-box/bot build`, `corepack pnpm db:runtime-check`.
- Трудности (2026-05-12): живой Telegram-прогон не автоматизируется из текущей среды и требует ручной проверки человеком в live-режиме бота.
- Решение (2026-05-12): оставили Этап 5 в статусе `В работе` до ручного прогона четырех пользовательских сценариев в Telegram.
- Выполнено (2026-05-12): исправлены `dev`-скрипты в `apps/api/package.json` и `apps/bot/package.json` (путь к `ts-node-dev` переведен на локальный `./node_modules/...`), чтобы локальный запуск через workspace снова работал.
- Self-check (2026-05-12): подтвержден живой локальный старт API через `corepack pnpm dev:api` (инициализация Nest, маршруты `health` и `clients`, успешное подключение Prisma к БД).
- Трудности (2026-05-12): при запуске бота в live-режиме (`BOT_DRY_RUN=false`) получена ошибка `Network request for 'getMe' failed!` — текущее окружение не имеет стабильного сетевого доступа к Telegram API.
- Решение (2026-05-12): для завершения Этапа 5 живые сценарии регистрации переносим на ручной прогон в окружении с доступом к Telegram.
- Выполнено (2026-05-12): в рабочем `.env` установлен `BOT_DRY_RUN=false`; до изменения бот запускался в безопасном dry-run и не мог отвечать в Telegram по `/start`.
- Диагностика сети (2026-05-12): `Resolve-DnsName api.telegram.org` успешен, но `Test-NetConnection api.telegram.org -Port 443` вернул `TcpTestSucceeded=False`.
- Диагностика сети (2026-05-12): `Invoke-WebRequest https://api.telegram.org` завершился ошибкой `Невозможно соединиться с удаленным сервером`.
- Вывод (2026-05-12): проблема текущего сбоя `/start` не в коде обработчиков, а в недоступности Telegram API из окружения.
- Повторная проверка (2026-05-12): блокер воспроизводится — `Invoke-WebRequest` снова не соединяется, `Test-NetConnection api.telegram.org -Port 443` снова `TcpTestSucceeded=False`.
- Повторная проверка после включения VPN (2026-05-12): из текущего терминала блокер сохраняется — `Test-NetConnection api.telegram.org -Port 443` = `False`, `Invoke-WebRequest https://api.telegram.org` = `Невозможно соединиться с удаленным сервером`, `corepack pnpm dev:bot` снова падает на `Network request for 'getMe' failed!`.
- Ручная проверка пользователя (2026-05-12): в локальном PowerShell получен успешный результат `Test-NetConnection api.telegram.org -Port 443` с `TcpTestSucceeded=True` (VPN-канал активен).
- Выполнено (2026-05-12): по подтверждению пользователя бот успешно отвечает в Telegram; этап регистрации принят и закрыт.
- Выполнено (2026-05-13): усилена логика повторного входа — если клиент уже зарегистрирован, бот очищает возможное «зависшее» состояние регистрации, автоматически подгружает профиль и открывает меню без повторного онбординга.

---

## Этап 6. Модель слотов и управление доступностью

Статус: `Завершен`

### Чек-лист текущего прогресса по этапу

- [x] Реализовать сущности и бизнес-правила слотов.
- [x] Реализовать открытие/закрытие слотов тренером.
- [x] Реализовать показ доступных слотов клиенту.
- [x] Исправить падение локальных watchdog-скриптов запуска (`watchdog:start/status/stop`).
- [x] Исправить клиентский вход для роли `admin`: запускать регистрацию перед клиентскими экранами, если клиент не зарегистрирован.
- [x] Восстановить локальный запуск PostgreSQL на `localhost:5432` (сейчас API падает на подключении к БД).
- [x] Устранить конфликт Telegram polling `409 Conflict` (одновременно работает другой экземпляр бота с тем же токеном).
- [x] Устранить ошибку `500` на `GET /clients/telegram/:telegramId` (проверка регистрации при входе в клиентский экран).
- [x] Выполнить ручной прогон API-операций открытия/закрытия слотов и проверить отображение слотов в клиентском меню бота.
  - [x] Выполнен автоматический прогон API/слотов через `corepack pnpm qa:stage6-slots`.
  - [x] Выполнить финальную ручную проверку того же сценария в Telegram-интерфейсе бота.

### Логи по этапу

- Выполнено (2026-05-12): добавлен новый API-модуль `slots` в `apps/api` с endpoint-ами `POST /slots/open`, `POST /slots/close`, `GET /slots/available`.
- Выполнено (2026-05-12): реализованы бизнес-правила слотов в `SlotsService` — часовые окна по 60 минут, защита от некорректных диапазонов, запрет закрытия `BOOKED`-слотов, переоткрытие ранее закрытых слотов.
- Выполнено (2026-05-12): реализован фильтр доступности для клиента с учетом настроек тренера (`bookingHorizonDays`, `sameDayBookingCutoff`) и московского часового пояса.
- Выполнено (2026-05-12): в `apps/bot` подключен `SlotsApiService`, экран `client-booking` теперь показывает реальные доступные слоты (МСК) из API.
- Выполнено (2026-05-12): для тестового режима тренера исправлен переход `Записаться` в клиентском меню — загрузка доступных слотов теперь выполняется и при роли `admin`, если открыт клиентский экран.
- Выполнено (2026-05-13): добавлен авто-ретрай подключения Telegram API в `apps/bot/src/main.ts` (бот больше не требует ручного перезапуска после временного обрыва VPN/сети).
- Self-check (2026-05-12): успешно выполнены `corepack pnpm typecheck`, `corepack pnpm --filter @tvoy-box/api build`, `corepack pnpm --filter @tvoy-box/bot build`.
- Self-check (2026-05-13): повторно успешно выполнен `corepack pnpm --filter @tvoy-box/bot build` после внедрения авто-ретрая.
- Runtime-check (2026-05-13): при недоступном Telegram API бот пишет `Telegram token validation failed` и автоматически делает повтор подключения каждые 15 секунд, не завершая процесс.
- Runtime-check (2026-05-13): повторно подтверждено, что при сетевой недоступности `api.telegram.org` бот не обрабатывает `/start`, но остается в цикле авто-переподключения и начинает работать сразу после восстановления канала.
- Выполнено (2026-05-13): выполнен принудительный перезапуск live-бота из корня проекта с явным `--dir`; по `logs/bot/runtime.jsonl` подтверждена повторная обработка `/start` (получено сообщение, вызван `/start`, открыт `admin-main`).
- Выполнено (2026-05-13): обновлен `navigation` для клиентских экранов (`client-*`) — перед открытием экрана проверяется регистрация, в том числе для роли `admin`; если клиент не зарегистрирован, запускается onboarding регистрации.
- Выполнено (2026-05-13): обновлены тексты клиентских экранов (`client-main`, `client-booking`) — убран устаревший текст про «каркас этапа 4», оставлены актуальные сообщения.
- Self-check (2026-05-13): успешно выполнены `corepack pnpm --filter @tvoy-box/bot typecheck` и `corepack pnpm --filter @tvoy-box/bot build` после правок навигации/регистрации.
- Диагностика (2026-05-13): подтверждено, что `Записаться` обрабатывается, но загрузка слотов падает с `fetch failed`, потому что API недоступен.
- Диагностика (2026-05-13): `corepack pnpm dev:api` падает на `PrismaClientInitializationError: Can't reach database server at localhost:5432` — локальная PostgreSQL не поднята.
- Диагностика (2026-05-13): выявлен конфликт `409 Conflict: terminated by other getUpdates request`; локальные дубликаты процессов очищены, но конфликт сохраняется, что указывает на второй poller с тем же токеном вне текущего процесса.
- Выполнено (2026-05-13): локальная PostgreSQL поднята через portable-сборку (`.tools/postgres`) в отдельном фоне; `corepack pnpm db:runtime-check` завершился `Prisma runtime-check: OK`.
- Выполнено (2026-05-13): API снова поднят (`/health = 200`), подтверждено по runtime-check и HTTP-проверке.
- Трудности (2026-05-13): запуск live-бота продолжает падать на `409 Conflict` даже после очистки локальных дубликатов, значит одновременно активен внешний экземпляр бота с этим же токеном (другая машина/сервер/IDE).
- Выполнено (2026-05-13): выполнена полная очистка локальных `ts-node-dev` процессов (`src/main.ts`), затем API и bot перезапущены по одному экземпляру; после перезапуска `Bot polling started` фиксируется без нового `409`.
- Решение (2026-05-13): причина конфликта была в локальных «осиротевших» процессах bot/API, оставшихся после предыдущих рестартов; добавлен порядок «чистый stop -> один start».
- Выполнено (2026-05-13): исправлена ошибка проверки регистрации клиента (`Clients API status 500`) — выполнен `prisma db push` для локальной БД `tvoy_box`, после чего `GET /clients/telegram/492732093` возвращает `200 {"found":false}`.
- Выполнено (2026-05-13): исправлены watchdog-скрипты в `scripts/dev` — устранен конфликт PowerShell-переменной `$PID` (параметры переименованы в `ProcessId`), добавлены stderr/stdout-логи процессов `api`/`bot`.
- Выполнено (2026-05-13): в `watchdog-loop.ps1` добавлен запуск через `corepack pnpm --dir "<root>" ...`, чтобы запуск не зависел от текущей директории пользователя.
- Трудности (2026-05-13): при запуске `corepack pnpm dev:bot` из `C:\Users\User` возникает `ERR_PNPM_NO_PKG_MANIFEST` (нет `package.json` в текущей папке).
- Решение (2026-05-13): запуск бота и API переведен на команды с явным `--dir` в watchdog, чтобы исключить ошибку из-за неверной рабочей директории.
- Self-check (2026-05-13): успешно выполнен `corepack pnpm typecheck` после правок скриптов watchdog.
- Трудности (2026-05-12): в процессе была ошибка TypeScript `TS4053` из-за неэкспортированных return-type интерфейсов в `slots.service.ts`.
- Решение (2026-05-12): интерфейсы результатов/DTO экспортированы, сборка и typecheck после исправления проходят успешно.
- Выполнено (2026-05-13): добавлен автоскрипт проверки `scripts/qa/stage6-slot-flow-check.mjs` и команда `corepack pnpm qa:stage6-slots` для безопасного прогона `open -> available -> close -> available`.
- Self-check (2026-05-13): `corepack pnpm qa:stage6-slots` выполнен успешно; подтверждено, что после закрытия слот исчезает из `GET /slots/available`.
- Решение (2026-05-13): оставляем последний подпункт Этапа 6 в статусе `не выполнен` до ручной проверки кликабельного сценария внутри Telegram.
- Выполнено (2026-05-13): изменена логика доступности слотов — если слот не создан вручную тренером, он считается доступным по умолчанию в рамках горизонта записи.
- Выполнено (2026-05-13): `POST /slots/close` теперь создает записи закрытых слотов даже для «неявных» (виртуальных) часов, чтобы закрытие работало без предварительного открытия.
- Self-check (2026-05-13): подтверждено, что `GET /slots/available` возвращает виртуальные слоты с id вида `virtual|<start>|<end>`.
- Исправлено (2026-05-13): устранена ошибка Telegram `BUTTON_DATA_INVALID` при входе в `Записаться` — сокращен формат виртуального `slotId` до `virtual|<epochMs>`, чтобы `callback_data` кнопки была < 64 байт.
- Выполнено (2026-05-13): ручная проверка в Telegram пройдена по подтверждению пользователя — слоты отображаются и сценарий `Записаться` работает корректно.

---

## Этап 7. Клиентская запись на слот

Статус: `Завершен`

### Чек-лист текущего прогресса по этапу

- [x] Реализовать создание заявки на слот.
- [x] Реализовать временную блокировку слота.
- [x] Реализовать проверки ограничений записи.
- [x] Внепланово улучшить UX выбора слота: сначала выбор даты, затем выбор времени (кнопками).
- [x] Сделать карточки дат/времени в сетке 2x3 с перелистыванием стрелками.

### Логи по этапу

- Выполнено (2026-05-13): добавлен API-модуль `bookings` с endpoint `POST /bookings/request` для создания клиентской заявки на выбранный слот.
- Выполнено (2026-05-13): при создании заявки слот переводится в `HELD` и блокируется до конца текущего дня (МСК), создается `booking` со статусом `PENDING`.
- Выполнено (2026-05-13): добавлены проверки ограничений — регистрация клиента, blacklist, недоступный/занятый слот, горизонт записи, same-day cutoff и правило «не больше одной активной записи в день».
- Выполнено (2026-05-13): в `apps/bot` добавлен `BookingsApiService`; в экране `client-booking` показаны кнопки слотов и отправка заявки по нажатию.
- Self-check (2026-05-13): успешно выполнены `corepack pnpm --filter @tvoy-box/api typecheck`, `corepack pnpm --filter @tvoy-box/bot typecheck`, `corepack pnpm --filter @tvoy-box/api build`, `corepack pnpm --filter @tvoy-box/bot build`.
- Runtime-check (2026-05-13): HTTP-прогон сценария подтвердил `POST /bookings/request`, блокировку слота (исчезает из `/slots/available`) и корректный отказ на повторной заявке (`409 Conflict`).
- Выполнено (2026-05-13): `POST /bookings/request` поддерживает запись на виртуальный слот — при выборе такого слота создается реальная запись `slot` в БД и далее применяется стандартная блокировка `HELD`.
- Выполнено (2026-05-13): в API добавлена обратная совместимость парсинга виртуальных id (старый ISO-формат и новый компактный epoch-формат), чтобы не ломать уже отправленные клиенту кнопки.
- Выполнено (2026-05-14): в `apps/bot/src/handlers/navigation.ts` клиентский экран `client-booking` переведен на двухшаговый сценарий выбора слота:
  - шаг 1: список доступных дат (МСК) кнопками;
  - шаг 2: после выбора даты показывается список доступного времени на эту дату кнопками.
- Выполнено (2026-05-14): добавлена группировка слотов по дате (`Europe/Moscow`) и отдельные callback-обработчики `slot:date:*` / `slot:time:*`.
- Выполнено (2026-05-14): для выбора дат и времени добавлена постраничная сетка `2 столбца x 3 строки` (6 карточек на экран) с кнопками `⬅️` / `➡️`.
- Выполнено (2026-05-14): добавлены отдельные callback-обработчики пагинации `slot:datepage:*` и `slot:timepage:*`, чтобы листание работало независимо для дат и времени.
- Решение (2026-05-14): лимит `6` выбран как компромисс между «красиво» и читаемо на мобильном экране Telegram без перегруза.
- Решение (2026-05-14): старый callback `slot:page:*` оставлен как совместимый fallback — при нажатии бот мягко переводит пользователя на новый интерфейс выбора даты.
- Self-check (2026-05-14): успешно выполнены `corepack pnpm --filter @tvoy-box/bot typecheck` и `corepack pnpm --filter @tvoy-box/bot build`.

---

## Этап 8. Админская обработка заявок

Статус: `Завершен`

### Чек-лист текущего прогресса по этапу

- [x] Реализовать подтверждение заявки.
  - [x] Отправлять клиенту уведомление о подтверждении.
- [x] Реализовать отклонение заявки.
  - [x] Отправлять клиенту уведомление об отклонении.
- [x] Реализовать предложение другого времени.
  - [x] Отправлять клиенту уведомление о предложенном новом времени.
  - [x] В карточке заявки показывать кликабельный профиль клиента вместо raw Telegram ID.
  - [x] В разделе заявок показывать все заявки со статусами, а не только `PENDING`.

### Логи по этапу

- Выполнено (2026-05-13): расширен API-модуль `bookings` — добавлены endpoint-ы `GET /bookings/pending`, `GET /bookings/:bookingId`, `POST /bookings/confirm`, `POST /bookings/reject`, `POST /bookings/propose-time`.
- Выполнено (2026-05-13): реализованы бизнес-правила админской обработки — подтверждение переводит заявку в `CONFIRMED`, слот в `BOOKED`, создается `training`; отклонение и предложение другого времени завершают заявку (`REJECTED` / `RESCHEDULED`) и освобождают удержанный слот.
- Выполнено (2026-05-13): добавлена автоматическая обработка истекших заявок (`PENDING -> EXPIRED`) при админских операциях и в списке заявок, с автоматическим освобождением `HELD`-слотов.
- Выполнено (2026-05-13): в `apps/bot` реализован рабочий админский экран заявок — список необработанных заявок, карточка заявки и кнопки `Подтвердить / Отклонить / Предложить другое время`.
- Выполнено (2026-05-13): для сценария «предложить другое время» добавлен кнопочный выбор альтернативных слотов из доступных клиенту.
- Выполнено (2026-05-13): добавлен runtime-check скрипт `scripts/qa/stage8-admin-bookings-flow-check.mjs` и команда `corepack pnpm qa:stage8-admin-bookings`.
- Трудности (2026-05-13): первый прогон runtime-check падал с `fetch failed` из-за того, что API не стартовал (ошибка DI `AppConfigService` в `BookingsModule`).
- Решение (2026-05-13): добавлен `AppConfigService` в providers модуля `bookings`, после чего API стабильно поднимается и сценарий проходит.
- Self-check (2026-05-13): успешно выполнены `corepack pnpm --filter @tvoy-box/api typecheck`, `corepack pnpm --filter @tvoy-box/api build`, `corepack pnpm --filter @tvoy-box/bot typecheck`, `corepack pnpm --filter @tvoy-box/bot build`.
- Runtime-check (2026-05-13): `corepack pnpm qa:stage8-admin-bookings` выполнен успешно (`Stage 8 admin booking flow check: OK`).
- Выполнено (2026-05-13): исправлено UX списка слотов для клиента — добавлена пагинация (`⬅️ Назад` / `Вперед ➡️`), чтобы можно было выбирать даты за пределами первых 8 слотов.
- Выполнено (2026-05-13): добавлено мгновенное уведомление тренеру/админу о новой заявке сразу после действия клиента (с кнопкой быстрого перехода в раздел `Заявки`).
- Выполнено (2026-05-13): добавлены уведомления клиенту при действиях тренера по заявке: подтверждение, отклонение и предложение другого времени.
- Выполнено (2026-05-13): очищены тестовые данные runtime-check (`Stage 6 Runtime Check`, `Stage 8 Runtime Check`) из локальной БД, чтобы не засорять рабочий список заявок.
- Выполнено (2026-05-13): в карточке заявки заменен блок `Telegram ID` на кликабельный профиль (`@username` с ссылкой), чтобы тренер мог быстро перейти к диалогу с клиентом.
- Выполнено (2026-05-13): API списка заявок расширен до всех статусов, в админском списке добавлены короткие статусные метки для каждой заявки.
- Решение (2026-05-13): для уже обработанных заявок в карточке оставлен режим просмотра без кнопок действий, чтобы избежать повторной обработки.
---

## Этап 9. Переносы и отмены подтвержденных тренировок

Статус: `Завершен`

### Чек-лист текущего прогресса по этапу

- [x] Реализовать перенос тренировки.
- [x] Реализовать отмену тренировки.
- [x] Реализовать уведомления обеих сторон.
  - [x] Выполнить ручной Telegram-прогон сценариев переноса/отмены и подтвердить тексты уведомлений у клиента и тренера.
  - [x] Устранить накопление служебных сообщений у клиента: после нового действия оставлять одно актуальное уведомление вместо длинной ленты старых системных сообщений.
  - [x] Улучшить UX списка заявок тренера: показывать статус заявки прямо на кнопке, чтобы не сверять со списком вручную.
  - [x] Устранить ложную ошибку «Не удалось обновить список» при нажатии `Обновить список`, когда данные фактически не изменились.
  - [x] Актуализировать текст раздела `Слоты` в панели тренера: убрать устаревшую ссылку на «будет реализовано на этапе 6».
  - [x] Реализовать клиентский ответ на предложение тренера по времени: `Согласен(на)` / `Не согласен(на)` с уведомлением тренеру о решении.
  - [x] Перевести сценарий `Перенести тренировку` (для подтвержденной тренировки) в формат предложения клиенту с обязательным выбором `Согласен(на)` / `Не согласен(на)`.
  - [x] Добавить тренеру ручное действие для зависших заявок (`Нов.вр.`): закрыть предложение/заявку без ручного вмешательства в БД.
  - [x] Убрать накопление служебных сообщений у тренера по действиям с заявками: оставлять только одно актуальное уведомление.
  - [x] Доработать клиентский раздел `Мои тренировки`: показывать реальные записи клиента со статусами.
  - [x] Добавить в `Мои тренировки` действия клиента: отмена и перенос подтвержденной тренировки.

### Логи по этапу

- Выполнено (2026-05-13): расширен API `bookings` для подтвержденных тренировок — добавлены endpoint-ы `POST /bookings/reschedule-training` и `POST /bookings/cancel-training`.
- Выполнено (2026-05-13): реализованы бизнес-операции для `CONFIRMED` заявок — перенос тренировки на новый слот (с освобождением старого слота) и отмена тренировки (с освобождением слота).
- Выполнено (2026-05-13): в админской карточке подтвержденной заявки добавлены действия `Перенести тренировку` и `Отменить тренировку`; для переноса реализован кнопочный выбор нового времени.
- Выполнено (2026-05-13): для переноса подтвержденной тренировки добавлена пагинация списка слотов (`⬅️ Назад` / `Вперед ➡️`), чтобы выбор нового времени был доступен на любые даты в горизонте записи, а не только на первой странице.
- Исправлено (2026-05-13): в переносе подтвержденной тренировки устранен дефект первой страницы (показывались только ближайшие слоты 13.05); теперь выбор нового времени листается по страницам и включает любые даты в доступном горизонте.
- Исправлено (2026-05-13): восстановлен клиентский UX после служебных уведомлений — в уведомлениях добавлены быстрые кнопки `Записаться` и `Открыть меню`, а в экране `Мои тренировки` добавлен прямой переход к новой записи.
- Выполнено (2026-05-13): добавлены уведомления клиенту при переносе/отмене подтвержденной тренировки и подтверждающие сообщения тренеру в Telegram.
- Выполнено (2026-05-13): клиентские служебные уведомления переведены в режим `replace` (удаляется предыдущее служебное сообщение и отправляется новое), чтобы после действий не копились старые системные сообщения внизу чата.
- Выполнено (2026-05-13): в разделе заявок тренера статус добавлен прямо в текст кнопки заявки (короткая цветовая метка), чтобы при выборе карточки сразу видеть состояние без сверки с верхним списком.
- Решение (2026-05-13): сохранен текущий список «все заявки (последние)», но добавлена понятная легенда статусов и дублирование статуса в кнопках для быстрого ориентирования.
- Исправлено (2026-05-13): в обработчике `Обновить список` добавлена отдельная обработка Telegram-ответа `message is not modified` — вместо алерта об ошибке теперь показывается нейтральное сообщение «Список уже актуален».
- Решение (2026-05-13): вынесена единая проверка `isMessageNotModifiedError(...)` и использована в местах обновления экранов, чтобы не считать «без изменений» технической ошибкой.
- Исправлено (2026-05-13): обновлен экран `admin-slots` — удален устаревший текст про будущую реализацию на этапе 6; добавено актуальное описание, что этап 6 уже завершен и текущий экран пока навигационный.
- Решение (2026-05-13): для `admin-slots` добавлен отдельный динамический view-builder в `navigation` (без риска поломать другие экраны), чтобы быстро актуализировать контент независимо от старого статического шаблона.
- Выполнено (2026-05-13): реализован полный цикл по предложенному времени: клиент получает уведомление с кнопками `Согласен(на)` / `Не согласен(на)` и может сразу отправить решение в боте.
- Выполнено (2026-05-13): добавлены API endpoint-ы `POST /bookings/proposal/accept` и `POST /bookings/proposal/decline` с проверками принадлежности заявки клиенту и валидности текущего статуса предложения.
- Выполнено (2026-05-13): при согласии клиента заявка переводится в `CONFIRMED`, создается/обновляется тренировка и отправляется уведомление тренеру; при отказе заявка переводится в `REJECTED` и тренер также получает уведомление.
- Исправлено (2026-05-13): для действия тренера `Перенести тренировку` убран принудительный перенос без выбора клиента; теперь клиент получает предложение нового времени с кнопками подтверждения/отказа.
- Выполнено (2026-05-13): при отказе клиента от переноса подтвержденной тренировки исходная тренировка сохраняется (статус заявки возвращается в `CONFIRMED`), при согласии перенос применяется и тренировка обновляется на новый слот.
- Решение (2026-05-13): унифицирован формат комментария с предложенным временем (`Предложено другое время (МСК): ...`), чтобы один и тот же механизм клиентского ответа работал и для новых заявок, и для переноса уже подтвержденных тренировок.
- Выполнено (2026-05-13): добавлен endpoint `POST /bookings/force-close` для ручного закрытия зависших кейсов тренером.
- Выполнено (2026-05-13): в карточке заявки со статусом `Нов.вр.` добавлена кнопка `Закрыть зависшее предложение`.
- Выполнено (2026-05-13): поведение ручного закрытия:
  - если предложение относится к подтвержденной тренировке, заявка возвращается в `Подтверждена` (текущее время сохраняется);
  - если это заявка без подтвержденной тренировки, заявка закрывается как `Отменена`.
- Решение (2026-05-13): для клиента отправляется понятное уведомление о результате ручного закрытия, чтобы не было «тихих» изменений.
- Исправлено (2026-05-13): в панели тренера служебные сообщения по заявкам (подтверждение, отклонение, отмена, предложение, ручное закрытие, ответ клиента) переведены в режим `replace` вместо накопления новых сообщений в чате.
- Решение (2026-05-13): добавлен трекер последнего служебного `message_id` для чата тренера и единый helper `sendOrReplaceAdminNotice(...)`, аналогично клиентскому сценарию.
- Трудности (2026-05-13): в текущей модели предложенное время хранится в текстовом комментарии тренера; для совместимости добавлен надежный парсер времени из формата `Предложено другое время (МСК): dd.mm.yyyy, hh:mm`.
- Решение (2026-05-13): выбрана инкрементальная реализация без миграции схемы БД на этом шаге, чтобы быстро восстановить рабочий пользовательский сценарий и не ломать текущие данные.
- Трудности (2026-05-13): Telegram не дает «очистить всю историю» чата программно как единое действие; поэтому реализован безопасный UX-подход через замену последнего служебного сообщения бота на актуальное.
- Решение (2026-05-13): добавлен локальный трекер последнего служебного `message_id` на чат клиента и единый helper отправки уведомлений, который используется во всех ключевых действиях (создание заявки, подтверждение, отклонение, перенос, отмена, предложение нового времени).
- Решение (2026-05-13): обработка переноса/отмены разрешена только для статуса `CONFIRMED`, для остальных статусов в карточке остаётся режим просмотра.
- Self-check (2026-05-13): успешно выполнены `corepack pnpm --filter @tvoy-box/api typecheck`, `corepack pnpm --filter @tvoy-box/api build`, `corepack pnpm --filter @tvoy-box/bot typecheck`, `corepack pnpm --filter @tvoy-box/bot build`.
- Self-check (2026-05-13): после внедрения `replace`-уведомлений повторно успешно выполнены `corepack pnpm --filter @tvoy-box/bot typecheck` и `corepack pnpm --filter @tvoy-box/bot build`.
- Self-check (2026-05-13): после добавления статусов в кнопки заявок успешно выполнены `corepack pnpm --filter @tvoy-box/bot typecheck` и `corepack pnpm --filter @tvoy-box/bot build`.
- Self-check (2026-05-13): после фикса ложного алерта `Не удалось обновить список` успешно выполнены `corepack pnpm --filter @tvoy-box/bot typecheck` и `corepack pnpm --filter @tvoy-box/bot build`.
- Self-check (2026-05-13): после актуализации текста экрана `Слоты` успешно выполнены `corepack pnpm --filter @tvoy-box/bot typecheck` и `corepack pnpm --filter @tvoy-box/bot build`.
- Self-check (2026-05-13): после добавления клиентского решения по предложенному времени успешно выполнены `corepack pnpm --filter @tvoy-box/api typecheck`, `corepack pnpm --filter @tvoy-box/api build`, `corepack pnpm --filter @tvoy-box/bot typecheck`, `corepack pnpm --filter @tvoy-box/bot build`.
- Self-check (2026-05-13): после перевода `Перенести тренировку` в режим клиентского подтверждения повторно успешно выполнены `corepack pnpm --filter @tvoy-box/api typecheck`, `corepack pnpm --filter @tvoy-box/api build`, `corepack pnpm --filter @tvoy-box/bot typecheck`, `corepack pnpm --filter @tvoy-box/bot build`.
- Self-check (2026-05-13): после добавления ручного закрытия зависших заявок успешно выполнены `corepack pnpm --filter @tvoy-box/api typecheck`, `corepack pnpm --filter @tvoy-box/api build`, `corepack pnpm --filter @tvoy-box/bot typecheck`, `corepack pnpm --filter @tvoy-box/bot build`.
- Self-check (2026-05-13): после перевода тренерских уведомлений в режим `replace` успешно выполнены `corepack pnpm --filter @tvoy-box/bot typecheck` и `corepack pnpm --filter @tvoy-box/bot build`.
- Выполнено (2026-05-13): по подтверждению пользователя проведена ручная проверка в Telegram — сценарии подтверждения/отклонения/предложения/переноса/ручного закрытия и тексты уведомлений работают корректно, этап закрыт.
- Внеплановая доработка (2026-05-14): реализован рабочий раздел `Мои тренировки` в клиентском меню (убрана заглушка):
  - список записей клиента со статусами;
  - карточка выбранной тренировки с деталями;
  - действие `Отменить тренировку`;
  - действие `Перенести тренировку` с выбором нового слота в 2 шага (дата -> время).
- Выполнено (2026-05-14): в API `bookings` добавлены клиентские endpoint-ы:
  - `GET /bookings/client/trainings`;
  - `POST /bookings/client/cancel-training`;
  - `POST /bookings/client/reschedule-training`.
- Решение (2026-05-14): клиентский перенос сделан как прямое подтвержденное изменение времени (без ручного вмешательства тренера), с проверкой доступности целевого слота и синхронизацией Google Calendar.
- Решение (2026-05-14): после клиентской отмены/переноса исходный слот возвращается в `CLOSED`, чтобы не нарушать модель «по умолчанию закрыто».
- Выполнено (2026-05-14): добавлены уведомления обеим сторонам по клиентской отмене/переносу, включая обновление клиентского приглашения `.ics` после переноса.
- Self-check (2026-05-14): после доработки `Мои тренировки` успешно выполнены:
  - `corepack pnpm --filter @tvoy-box/api typecheck`;
  - `corepack pnpm --filter @tvoy-box/api build`;
  - `corepack pnpm --filter @tvoy-box/bot typecheck`;
  - `corepack pnpm --filter @tvoy-box/bot build`.

---

## Этап 10. Сценарий «нет подходящего времени»

Статус: `Завершен`

### Чек-лист текущего прогресса по этапу

- [x] Реализовать запрос без слота.
- [x] Сохранять желаемые дни/время.
- [x] Отправлять запрос тренеру.
  - [x] Выполнить ручной Telegram-прогон сценария (клиент заполняет шаги, тренер получает уведомление с данными запроса).

### Логи по этапу

- Выполнено (2026-05-13): добавлен API-модуль `no-slot-requests` в `apps/api` с endpoint `POST /no-slot-requests/request`.
- Выполнено (2026-05-13): реализовано сохранение запроса без слота в БД (`NoSlotRequest`) с полями `preferredDays`, `preferredTime`, `clientComment`, статусом `NEW` и привязкой к клиенту.
- Выполнено (2026-05-13): добавлен клиентский сценарий в боте для экрана `Нет подходящего времени` — пошаговый ввод: желаемые дни -> желаемое время (или пропуск) -> комментарий (или пропуск).
- Выполнено (2026-05-13): добавлены callback-действия `noslot:start`, `noslot:skip-time`, `noslot:skip-comment`, `noslot:cancel` и обработка текстовых шагов.
- Выполнено (2026-05-13): после создания запроса отправляется подтверждение клиенту и уведомление тренеру/админу с данными запроса (клиент, username, телефон, дни, время, комментарий).
- Исправлено (2026-05-13): устранено ложное сообщение клиенту `Не удалось отправить тренеру запрос` в случае, когда запрос уже сохранен, но упала отправка уведомления одному из получателей.
- Решение (2026-05-13): создание запроса и отправка уведомления тренеру разделены; при сбое уведомления запрос не откатывается, клиент видит корректный статус сохранения.
- Решение (2026-05-13): для тренерских уведомлений по запросам без слота выбран формат `replace` (одно актуальное служебное сообщение), чтобы не засорять чат.
- Self-check (2026-05-13): успешно выполнены `corepack pnpm --filter @tvoy-box/api typecheck`, `corepack pnpm --filter @tvoy-box/api build`, `corepack pnpm --filter @tvoy-box/bot typecheck`, `corepack pnpm --filter @tvoy-box/bot build`.
- Self-check (2026-05-13): после фикса ложного сообщения в no-slot сценарии успешно выполнены `corepack pnpm --filter @tvoy-box/bot typecheck` и `corepack pnpm --filter @tvoy-box/bot build`.
- Выполнено (2026-05-13): по подтверждению пользователя ручной Telegram-прогон сценария `Нет подходящего времени` прошел успешно, этап закрыт.

---

## Этап 11. Интеграция с Google Calendar

Статус: `Завершен`

### Чек-лист текущего прогресса по этапу

- [x] Подключить Google Calendar API.
- [x] Реализовать создание/обновление/отмену событий.
- [x] Реализовать ручную пересинхронизацию.
- [x] Выполнить живой прогон Google Calendar с реальными credentials (confirm/reschedule cancel/resync) и зафиксировать результат.
- [x] Проверить, что после смены `GOOGLE_CALENDAR_ID` события создаются именно в целевом календаре пользователя.
- [x] Удалить `Telegram ID` из описания события в календаре.
- [x] Сделать поле `Username` кликабельным (`https://t.me/<username>`).
- [x] Настроить напоминания в событии: за `1 день` и за `1 час`.
- [x] Убрать из карточки события служебные идентификаторы `Booking ID` и `Training ID`.

### Логи по этапу

- Выполнено (2026-05-13): добавлен новый модуль `apps/api/src/modules/google-calendar` с service account OAuth 2.0 (JWT bearer) и вызовами Google Calendar API (`events.insert`, `events.patch`, `events.delete`).
- Выполнено (2026-05-13): в `bookings` внедрена календарная синхронизация:
  - подтверждение заявки создает событие в Google Calendar и сохраняет `training.calendarEventId`;
  - принятие клиентом предложенного времени обновляет существующее событие или создает новое, если события не было;
  - отмена подтвержденной тренировки отменяет событие в Google Calendar.
- Выполнено (2026-05-13): реализован ручной endpoint пересинхронизации `POST /bookings/resync-calendar` (доступ только trainer/admin).
- Выполнено (2026-05-13): в админском интерфейсе бота добавлена кнопка `Пересинхронизировать календарь` и обработчик `adm:resync:*`.
- Выполнено (2026-05-13): добавено логирование синхронизаций в `CalendarSyncLog` (`SUCCESS/FAILED`, операции `CREATE/UPDATE/CANCEL/RESYNC`, `externalEventId`, payload).
- Self-check (2026-05-13): успешно выполнены `corepack pnpm --filter @tvoy-box/api typecheck`, `corepack pnpm --filter @tvoy-box/api build`, `corepack pnpm --filter @tvoy-box/bot typecheck`, `corepack pnpm --filter @tvoy-box/bot build`.
- Трудности (2026-05-13): при строгой типизации TypeScript были ошибки по nullable-связям `training` и `payload` JSON в Prisma.
- Решение (2026-05-13): добавлены явные проверки `training` перед синхронизацией, корректные типы `Prisma.InputJsonObject`, и fallback-обработка `404` Google Calendar для сценария ручного resync.
- Примечание (2026-05-13): автоматический живой runtime-прогон с реальным Google API не выполнен в текущей среде (нужны валидные service account credentials и доступ к целевому календарю).
- Выполнено (2026-05-13): пользователь добавил service account в доступ календаря с правами `Изменение мероприятий`, в `.env` заполнены `GOOGLE_SERVICE_ACCOUNT_EMAIL` и `GOOGLE_PRIVATE_KEY`.
- Выполнено (2026-05-13): проверка формата `.env` пройдена — email корректного формата `*.iam.gserviceaccount.com`, private key содержит `BEGIN/END` и `\\n`.
- Выполнено (2026-05-13): добавлен `GOOGLE_CALENDAR_SYNC_MODE=real` в рабочий `.env`.
- Runtime-check (2026-05-13): live-сценарий `request -> confirm` из sandbox падает на `500 Internal server error` в шаге `POST /bookings/confirm`.
- Диагностика сети (2026-05-13): отдельный Node-check из sandbox показал блокировку исходящего HTTPS к Google (`connect EACCES ...:443`).
- Runtime-check вне sandbox (2026-05-13): OAuth token service account успешно получен (`https://oauth2.googleapis.com/token`, `HTTP 200`, `access_token` получен), credentials валидны.
- Вывод (2026-05-13): основной блокер live E2E-проверки этапа 11 — сетевые ограничения sandbox для исходящего доступа к Google API; код и credentials готовы, требуется прогон в окружении без этой блокировки.
- Диагностика инцидента (2026-05-13): по логам бота подтвержден отказ на шаге `adm:confirm:*` — `Bookings API responded with status 500: {"statusCode":500,"message":"Internal server error"}`.
- Диагностика сети пользователя (2026-05-13): `Test-NetConnection oauth2.googleapis.com -Port 443` вернул `TcpTestSucceeded=False` (канал к Google OAuth из окружения недоступен).
- Исправлено (2026-05-13): в `google-calendar.service` добавлена явная обработка сетевых исключений (`fetch failed`) с понятными сообщениями `ServiceUnavailableException` для OAuth/token и Calendar API вызовов.
- Исправлено (2026-05-13): в боте улучшено сообщение для тренера — при `Internal server error` по confirm теперь показывается причина про сбой синхронизации Google Calendar, вместо общего «Не удалось обработать заявку».
- Self-check (2026-05-13): после фикса ошибок синхронизации успешно выполнены `corepack pnpm --filter @tvoy-box/api typecheck` и `corepack pnpm --filter @tvoy-box/bot typecheck`.
- Выполнено (2026-05-13): выполнен чистый перезапуск runtime-процессов `api` и `bot`; после перезапуска подтвержден сетевой путь к Google OAuth и снят блокер `500` на confirm.
- Runtime-check (2026-05-13): успешный живой прогон `request -> confirm -> resync-calendar -> cancel-training`:
  - `confirm` вернул `status=confirmed`;
  - `resync-calendar` вернул `status=resynced`;
  - `cancel-training` вернул `status=cancelled`.
- Решение (2026-05-13): причиной инцидента оказался не бизнес-код заявок, а сетевой/процессный runtime-контекст; после стабилизации запуска подтверждение снова работает штатно.
- Диагностика (2026-05-13): проверка `Test-NetConnection oauth2.googleapis.com -Port 443` показала разницу контуров: в sandbox соединение `False`, вне sandbox `TcpTestSucceeded=True`.
- Диагностика (2026-05-13): обнаружено, что ранее созданный `calendarEventId` (`qn7v9u...`) не найден в календаре `findirtobolova@gmail.com` (`Google API 404`), то есть событие было создано до актуального `GOOGLE_CALENDAR_ID`.
- Выполнено (2026-05-13): запущен `POST /bookings/resync-calendar` для проблемной брони `cmp43u2ko000jvr6oai9ulctq`; событие пересоздано, `training.calendarEventId` обновлен на новый id.
- Runtime-check (2026-05-13): прямой запрос к Google Calendar API по новому `eventId` вернул `HTTP 200`; событие подтверждено в календаре `findirtobolova@gmail.com`.
- Решение (2026-05-13): для уже существующих подтвержденных тренировок после смены `GOOGLE_CALENDAR_ID` обязательно выполнять `resync-calendar`, чтобы привязать их к правильному календарю без ручного редактирования БД.
- Выполнено (2026-05-14): обновлен `buildCalendarEventBody` в `google-calendar.service` — из описания события убран `Telegram ID`.
- Выполнено (2026-05-14): из вызовов `bookings -> google-calendar` удалена передача `clientTelegramId`, чтобы лишний идентификатор вообще не передавался в слой календаря.
- Выполнено (2026-05-14): `Username` в карточке события переведен в формат ссылки `https://t.me/<username>`, чтобы можно было написать клиенту в один клик.
- Выполнено (2026-05-14): в тело события добавлены кастомные reminders (`useDefault=false`) с двумя popup-напоминаниями: `1440` и `60` минут до тренировки.
- Выполнено (2026-05-14): из описания события удалены `Booking ID` и `Training ID`, оставлены только полезные для тренера поля (клиент, телефон, username, комментарий).
- Решение (2026-05-14): для `updateEvent` переключен метод `PATCH -> PUT`, чтобы гарантированно перезаписывать reminders для уже существующих событий.
- Runtime-check (2026-05-14): выполнен `POST /bookings/resync-calendar` для брони `cmp3r60aj0002vr8gic3i3546`; прямой запрос к Google Calendar API по `eventId=isllg23ur0ht4ilkt75balple8` вернул:
  - `description` без `Booking ID`/`Training ID`;
  - `reminders.useDefault=false`, `overrides=[1440, 60]`.
- Self-check (2026-05-14): успешно выполнены `corepack pnpm --filter @tvoy-box/api typecheck` и `corepack pnpm --filter @tvoy-box/api build`.
- Решение (2026-05-14): для уже существующих событий применяется `resync-calendar`, чтобы обновления карточки и reminders гарантированно дошли до Google Calendar.

---

## Этап 12. Добавление записи в календарь клиента

Статус: `Завершен`

### Чек-лист текущего прогресса по этапу

- [x] Реализовать выдачу приглашения клиенту.
- [x] Проверить корректность даты/времени и открытия.
- [x] Добавить напоминания в клиентское приглашение: за 1 день и за 1 час.

### Логи по этапу

- Выполнено (2026-05-14): в `apps/bot/src/handlers/navigation.ts` добавлена генерация и отправка клиенту файла-приглашения `.ics` (формат iCalendar).
- Выполнено (2026-05-14): отправка приглашения включена в два сценария:
  - после подтверждения заявки тренером (`adm:confirm`);
  - после принятия клиентом предложенного переноса (`cli:prop:acc`).
- Выполнено (2026-05-14): в `.ics` добавлены два напоминания (`VALARM`) — за 1 день и за 1 час.
- Решение (2026-05-14): для совместимости с разными календарями время в `.ics` формируется в UTC (`DTSTART/DTEND/DTSTAMP`), чтобы корректно импортировалось в Google/Apple/Outlook.
- Трудности (2026-05-14): при сбое отправки клиенту файла нельзя ломать уже завершенное бизнес-действие (подтверждение/перенос), поэтому отправка приглашения обернута в отдельный `try/catch` с логированием.
- Self-check (2026-05-14): успешно выполнены `corepack pnpm --filter @tvoy-box/bot typecheck` и `corepack pnpm --filter @tvoy-box/bot build`.
- Уточнение (2026-05-14): в `apps/api/src/modules/google-calendar/google-calendar.service.ts` напоминания события уже формируются как `overrides=[1440, 60]` с `useDefault=false`. При работе через `Service Account` напоминания в Google Calendar являются пользовательскими и могут отображаться у владельца календаря по его дефолтным настройкам (например, `30 минут`), несмотря на значения в событии, созданном сервисным аккаунтом.
- Решение (2026-05-14): для строгого управления напоминаниями именно в календаре тренера нужны OAuth-токены самого тренера (создание/обновление событий от его пользователя). В текущей схеме `Service Account` это ограничение Google Calendar API.

---

## Этап 13. Админские настройки, черный список и поиск

Статус: `Завершен`

### Чек-лист текущего прогресса по этапу

- [x] Настройки горизонта и day-to-day ограничений.
- [x] Поиск клиента по имени/телефону.
- [x] Черный список.
  - [x] Показ списка клиентов в ЧС.
  - [x] Удаление клиента из ЧС кнопкой в боте.
- [x] Переработать раздел `Даты и время`: выбор дат и точного времени слотов из админ-панели.
- [x] Добавить UX-полировку раздела `Даты и время` (массовые операции по диапазону дат, итоговый текстовый статус по выбранной дате).
  - [x] Добавить шаблон на весь горизонт: выбор дней недели + часов, с массовым применением без настройки каждого дня отдельно.
- [x] Убрать ограничение «только одна заявка на день» для клиента.
- [x] Добавить `Закрытие периодов` с вводом причины и показом причины клиенту.
- [x] Перевести мастер закрытия на формат `только даты` (`дата начала` + `дата окончания` + `причина`).
- [x] Добавить список уже закрытых периодов с причиной в `Панель админа -> Закрытие периодов`.
- [x] Добавить отмену закрытия по конкретному периоду из списка.
- [x] Стабилизировать `/start` для админа без Telegram-ошибки `text must be non-empty`.
- [x] Восстановить кириллицу и читаемые подписи в `Панель админа -> Даты и время`.
- [x] Упростить `Панель админа -> Даты и время`: убрать текстовую сводку и перейти на цветовые индикаторы слотов/дат.
- [x] Привести фактические данные к модели `по умолчанию закрыто` (закрыть накопившиеся `OPEN`-слоты и убрать авто-возврат в `OPEN` после отмены/переноса).

### Логи по этапу

- Выполнено (2026-05-14): добавлен API-модуль `trainer-settings`:
  - `GET /trainer-settings/current` — получить текущие настройки;
  - `POST /trainer-settings/update` — обновить `bookingHorizonDays` и/или `sameDayBookingCutoff`.
- Выполнено (2026-05-14): добавлена валидация и ограничения значений:
  - горизонт записи: `1..60` дней;
  - ограничение день-в-день: `0..23` часов.
- Выполнено (2026-05-14): модуль `TrainerSettingsModule` подключен в `AppModule`, настройки доступны в общем API.
- Выполнено (2026-05-14): в `apps/bot` реализован рабочий экран `admin-settings`:
  - показ текущих значений настроек;
  - кнопки быстрого изменения (`±1/±7` для горизонта, `±1/±3` для cutoff);
  - кнопка обновления экрана.
- Выполнено (2026-05-14): интерфейс раздела настроек переработан в формат «Панель админа» с понятными пунктами меню:
  - `Даты и время`;
  - `Период для записи`;
  - `Закрытие периодов`;
  - `Запись заранее`;
  - `Черный список`.
- Выполнено (2026-05-14): в пункте `Период для записи` добавлены пресеты `1/2/3/4 недели` (7/14/21/28 дней) с сохранением в `bookingHorizonDays`.
- Выполнено (2026-05-14): в пункте `Запись заранее` добавлены пресеты `0/1/2/3/6/12 часов` с сохранением в `sameDayBookingCutoff`.
- Выполнено (2026-05-14): в API клиентов добавлены админ-операции черного списка:
  - `GET /clients/blacklist` — список клиентов в ЧС;
  - `POST /clients/blacklist/remove` — удаление клиента из ЧС.
- Выполнено (2026-05-14): в `Панель админа -> Черный список` реализованы:
  - отображение списка клиентов с причиной блокировки;
  - кнопка удаления каждого клиента из ЧС;
  - кнопка обновления списка.
- Выполнено (2026-05-14): стартовый экран для тренера/админа упрощен по запросу — после `/start` показываются только карточки:
  - `Заявки`;
  - `Панель админа`;
  - `Открыть клиентское меню` (временный пункт, удалить на финальном этапе).
- Инцидент (2026-05-14): Telegram отклонял «пустые» служебные символы (`U+200B`, `U+2800`) на `/start` с ошибкой `400: text must be non-empty`, из-за чего пользователь видел «Произошла ошибка».
- Решение (2026-05-14): в админ-экране (`/start` и навигация) заменен невидимый текст на короткий явный `Выберите раздел ↓`; это совместимо с Telegram API и не ломает UX.
- Self-check (2026-05-14): после фикса повторно успешно выполнены `corepack pnpm --filter @tvoy-box/bot typecheck` и `corepack pnpm --filter @tvoy-box/bot build`.
- Улучшение UX (2026-05-14): при пустом черном списке бот явно сообщает, что проверка удаления недоступна, потому что в ЧС пока нет клиентов.
- Решение (2026-05-14): удаление из ЧС сделано идемпотентным — если клиент уже не в ЧС, бот показывает корректный ответ без падения сценария.
- Решение (2026-05-14): для понятности пользователю убраны технические `+/-`-кнопки, вместо них добавлены пресеты «человеческими» значениями.
- Инцидент/фикс (2026-05-14): экран `Настройки` не открывался из-за runtime-ошибки DI в API (`TrainerSettingsService` не мог получить `AppConfigService` в `TrainerSettingsModule`), бот видел `fetch failed`.
- Решение (2026-05-14): в `TrainerSettingsModule` добавлены `PrismaModule` в `imports` и `AppConfigService` в `providers`; после фикса endpoint `GET /trainer-settings/current` отвечает корректно.
- Решение (2026-05-14): изменение параметров сделано через кнопки в Telegram без ручного ввода текста, чтобы снизить риск ошибок у администратора.
- Self-check (2026-05-14): успешно выполнены `corepack pnpm --filter @tvoy-box/api typecheck`, `corepack pnpm --filter @tvoy-box/api build`, `corepack pnpm --filter @tvoy-box/bot typecheck`, `corepack pnpm --filter @tvoy-box/bot build`.
- Выполнено (2026-05-14): реализован поиск клиента в API:
  - `GET /clients/search?trainerTelegramId=...&q=...&limit=...`;
  - поиск по `fullName`, `username`, `phone` (в том числе по цифрам телефона).
- Выполнено (2026-05-14): в `Панель админа` добавлен пункт `Поиск клиента`, в боте реализован режим ввода запроса (имя/телефон) и вывод найденных клиентов с кратким статусом.
- Трудность (2026-05-14): в Telegram для поиска нужен текстовый ввод, поэтому вместо только кнопок добавлен отдельный «режим поиска» с кнопкой завершения.
- Решение (2026-05-14): режим поиска включается/выключается явными кнопками, чтобы не мешать остальным сценариям меню.
- Self-check (2026-05-14): после внедрения поиска повторно успешно выполнены `corepack pnpm --filter @tvoy-box/api typecheck`, `corepack pnpm --filter @tvoy-box/api build`, `corepack pnpm --filter @tvoy-box/bot typecheck`, `corepack pnpm --filter @tvoy-box/bot build`.
- Runtime-check (2026-05-14): endpoint поиска отвечает корректно:
  - `GET /clients/search?...&q=an` → `200` (`status=ok`);
  - `GET /clients/search?...&q=a` → `400` (контроль минимальной длины запроса).
- Выполнено (2026-05-14): реализовано `Закрытие периодов` в админ-панели:
  - выбор длительности закрытия (`1/3/7/14` дней) кнопками;
  - ввод причины текстом в чат;
  - сохранение закрытия через `POST /slots/close` с интервалом и причиной.
- Доработка (2026-05-14): добавлен выбор даты начала закрытия периода (сейчас / через 1 неделю / через 2 недели / через 3 недели), чтобы можно было закрывать периоды не только «с текущего момента».
- Решение (2026-05-14): сценарий закрытия периода переведен на 3 шага:
  - выбор даты начала;
  - выбор длительности;
  - ввод причины.
- Доработка UX (2026-05-14): причина закрытия периода теперь показывается клиенту не только когда слотов `0`, но и при частичной доступности (когда часть дат открыта, а часть закрыта).
- Решение (2026-05-14): в экране записи клиента добавлен информационный блок о закрытом периоде (диапазон + причина) поверх списка доступных дат/времени.
- Доработка (2026-05-14): в `Закрытие периодов` добавлен выбор произвольной даты начала:
  - ручной ввод даты в формате `ДД.ММ.ГГГГ`.
- Доработка (2026-05-14): убран шаг выбора «на сколько дней/часов».
- Решение (2026-05-14): закрытие/отмена периода переведены в формат `только даты`:
  - дата начала;
  - дата окончания (включительно);
  - причина (только для закрытия периода).
- Выполнено (2026-05-14): добавлена функция `Отменить закрытие периода`:
  - новый API endpoint `POST /slots/reopen`;
  - в боте отдельный режим `Отменить закрытие` с выбором даты начала и длительности.
- Решение (2026-05-14): закрытие/отмена сделаны единым мастером с режимами `close` и `reopen`, чтобы не дублировать логику и уменьшить риск ошибок.
- Инцидент/фикс (2026-05-14): при вводе `start` в активном мастере закрытия бот мог оставаться в контексте мастера.
- Решение (2026-05-14): добавлен явный сброс состояний мастера (`close-period` и `search`) по `start`/`/start`, для администратора `start` теперь сразу открывает общее админ-меню.
- Доработка (2026-05-14): добавлен сброс временных состояний при переходах по `screen:*`, чтобы исключить залипание мастера при входе в другие разделы.
- Выполнено (2026-05-14): добавлен API `GET /slots/closure-info` для клиента (причина, период закрытия, количество закрытых слотов в горизонте записи).
- Выполнено (2026-05-14): в клиентском экране записи при отсутствии слотов показывается причина закрытия периода и дата «до какого времени закрыто», если причина задана тренером.
- Трудность (2026-05-14): в Telegram нельзя удобно вводить структуру данных кнопками для причины, поэтому применен двухшаговый режим: сначала выбор длительности кнопкой, затем отдельное текстовое сообщение с причиной.
- Решение (2026-05-14): режимы `поиск клиента` и `закрытие периодов` сделаны взаимоисключающими, чтобы пользовательские сообщения не перехватывались двумя сценариями одновременно.
- Self-check (2026-05-14): после реализации закрытия периодов успешно выполнены `corepack pnpm --filter @tvoy-box/api typecheck`, `corepack pnpm --filter @tvoy-box/api build`, `corepack pnpm --filter @tvoy-box/bot typecheck`, `corepack pnpm --filter @tvoy-box/bot build`.
- Self-check (2026-05-14): после доработки выбора даты начала закрытия успешно выполнены `corepack pnpm --filter @tvoy-box/bot typecheck` и `corepack pnpm --filter @tvoy-box/bot build`.
- Self-check (2026-05-14): после доработки отображения причин при частично закрытых периодах успешно выполнены `corepack pnpm --filter @tvoy-box/bot typecheck` и `corepack pnpm --filter @tvoy-box/bot build`.
- Self-check (2026-05-14): после добавления произвольной даты старта и отмены закрытия успешно выполнены `corepack pnpm --filter @tvoy-box/api typecheck`, `corepack pnpm --filter @tvoy-box/api build`, `corepack pnpm --filter @tvoy-box/bot typecheck`, `corepack pnpm --filter @tvoy-box/bot build`.
- Self-check (2026-05-14): после фикса `start` и расширения быстрых дат успешно выполнены `corepack pnpm --filter @tvoy-box/bot typecheck` и `corepack pnpm --filter @tvoy-box/bot build`.
- Self-check (2026-05-14): после перевода мастера закрытия на формат `только даты` успешно выполнены `corepack pnpm --filter @tvoy-box/bot typecheck` и `corepack pnpm --filter @tvoy-box/bot build`.
- Доработка UX (2026-05-14): в сценарии `Закрытие периодов` шаги сделаны явными и последовательными:
  - `Шаг 1 из 3` — дата начала;
  - `Шаг 2 из 3` — дата окончания;
  - `Шаг 3 из 3` — причина закрытия.
- Доработка валидации (2026-05-14): ввод даты теперь принимает форматы `ДД.ММ.ГГГГ` и `ДД.ММ.ГГ`, при этом добавено явное различение ошибок:
  - неверный формат;
  - несуществующая календарная дата (например `15.15.26`).
- Выполнено (2026-05-14): экран `Панель админа -> Закрытие периодов` переведен в режим списка периодов:
  - показываются текущие закрытые периоды с причиной;
  - кнопка `Закрыть новый период`;
  - отдельные кнопки `Отменить: ...` по каждому закрытому периоду;
  - `Обновить список`.
- Решение (2026-05-14): отмена закрытия периода теперь выполняется по выбранной карточке периода, без ручного ввода длительности/интервала.
- Трудность (2026-05-14): в коде остались ссылки на старую клавиатуру ввода периода, что создавало риск «залипания» сценария; ссылки удалены, мастер унифицирован на единую клавиатуру шага.
- Self-check (2026-05-14): после финальных правок закрытия периодов успешно выполнены:
  - `corepack pnpm --filter @tvoy-box/bot typecheck`;
  - `corepack pnpm --filter @tvoy-box/bot build`;
  - `corepack pnpm --filter @tvoy-box/api typecheck`;
  - `corepack pnpm --filter @tvoy-box/api build`.
- Инцидент/фикс (2026-05-14): при нажатии `Обновить список` в `Панель админа -> Закрытие периодов` Telegram мог вернуть `message is not modified`, из-за чего появлялся алерт `Не удалось обновить список`.
- Решение (2026-05-14): добавлена отдельная обработка `message is not modified` для `adm:closeperiod:refresh`; теперь бот показывает нейтральное сообщение `Список уже актуален` без ошибки.
- Self-check (2026-05-14): после фикса обновления списка успешно выполнены `corepack pnpm --filter @tvoy-box/bot typecheck` и `corepack pnpm --filter @tvoy-box/bot build`.
- Инцидент/фикс данных (2026-05-14): обнаружен период закрытия с некорректной границей старта (`17.05 13:00` вместо `17.05 00:00`), из-за чего у клиента оставались утренние слоты на закрытую дату.
- Решение (2026-05-14): в API (`slots.close`) добавлена нормализация диапазона закрытия периода к полным московским суткам (дата начала с `00:00`, дата окончания как `end exclusive` по границе суток), чтобы такие сдвиги больше не повторялись.
- Выполнено (2026-05-14): выполнен аккуратный runtime-repair существующих длинных закрытых периодов с «сдвинутыми» границами (без затрагивания коротких одиночных закрытий):
  - обновлено `12` слотов в БД для выравнивания периода;
  - `1` занятый (`BOOKED`) слот пропущен как неизменяемый.
- Выполнено (2026-05-14): начат и реализован следующий подэтап `Даты и время` в панели админа:
  - добавлен API `GET /slots/trainer-grid` для получения сетки слотов тренера по диапазону;
  - в боте реализован выбор даты (страницы дат по горизонту записи);
  - внутри даты реализовано управление по каждому часу (открыть/закрыть), а также кнопки `Открыть весь день` / `Закрыть весь день`.
- Доработка UX (2026-05-14): в списке дат раздела `Даты и время` добавлена явная индикация состояния каждой даты:
  - бейдж статуса (`🟢` все открыто, `⚪` все закрыто, `🟡` смешано, `🔴` есть занятые);
  - счетчик открытых часов в формате `open/total` прямо на кнопке даты.
- Доработка UX (2026-05-14): в верхнем тексте раздела `Даты и время` добавлена сводка по горизонту (сколько дат открытых, закрытых, смешанных и с занятыми слотами).
- Self-check (2026-05-14): после доработки индикации дат успешно выполнены `corepack pnpm --filter @tvoy-box/bot typecheck` и `corepack pnpm --filter @tvoy-box/bot build`.
- Диагностика (2026-05-14): по жалобе «клиент видит странные/неожиданные времена» проверены фактические слоты в БД и настройки; выявлено, что у тренера включено `sameDayBookingCutoff=2`, из-за чего ближайшие часы на сегодня скрываются для клиента.
- Доработка UX (2026-05-14): в клиентском экране записи добавлено явное пояснение про ограничение `запись заранее` (например: минимум за `2 ч` до старта), чтобы поведение слотов было прозрачным.
- Доработка UX (2026-05-14): блок про закрытый период теперь показывается на шаге выбора времени только если выбранная клиентом дата реально пересекается с закрытым диапазоном; для не затронутых дат лишний блок не показывается.
- Self-check (2026-05-14): после фикса клиентского отображения успешно выполнены `corepack pnpm --filter @tvoy-box/bot typecheck` и `corepack pnpm --filter @tvoy-box/bot build`.
- Выполнено (2026-05-14): удалено бизнес-ограничение «одна заявка на один день»:
  - в API (`bookings.service`) убраны проверки `assertClientHasNoSameDayBooking` при создании заявки и принятии переноса;
  - в боте удалены пользовательские тексты для ошибки `Only one booking per day is allowed` как устаревшие.
- Решение (2026-05-14): оставлены только актуальные ограничения записи (горизонт записи, запись заранее в часах, доступность слота), без дневного лимита по числу заявок.
- Self-check (2026-05-14): после удаления дневного лимита успешно выполнены:
  - `corepack pnpm --filter @tvoy-box/api typecheck`;
  - `corepack pnpm --filter @tvoy-box/api build`;
  - `corepack pnpm --filter @tvoy-box/bot typecheck`;
  - `corepack pnpm --filter @tvoy-box/bot build`.
- Решение по плану (2026-05-14): подэтап `Выгрузка записей` удален из Этапа 13 по согласованию, функциональность выгрузки в MVP не реализуется.
- Решение (2026-05-14): при закрытии через `Даты и время` используется единая причина `Закрыто тренером через панель даты и времени`, чтобы клиенту и в логах было понятно происхождение ограничения.
- Self-check (2026-05-14): после фикса закрытой даты и добавления подэтапа `Даты и время` успешно выполнены:
  - `corepack pnpm --filter @tvoy-box/api typecheck`;
  - `corepack pnpm --filter @tvoy-box/api build`;
  - `corepack pnpm --filter @tvoy-box/bot typecheck`;
  - `corepack pnpm --filter @tvoy-box/bot build`.
- Выполнено (2026-05-14): в разделе `Панель админа -> Даты и время` добавлен режим массового изменения по диапазону дат:
  - шаг 1: выбор даты начала;
  - шаг 2: выбор даты окончания;
  - шаг 3: выбор действия (`Открыть диапазон` / `Закрыть диапазон`).
- Выполнено (2026-05-14): добавлен итоговый статус после применения диапазона:
  - диапазон в человекочитаемом виде;
  - число дней в диапазоне;
  - итог по слотам (`открыто/закрыто/занято`) после операции.
- Доработка UX (2026-05-14): в списке дат добавлена кнопка `Массово по диапазону`, а в мастере диапазона — безопасные кнопки `Изменить даты` и `Отмена`.
- Решение (2026-05-14): состояния временных мастеров синхронизированы (сброс по `/start`, переходам экранов и отмене), чтобы избежать залипания сценариев.
- Self-check (2026-05-14): после внедрения массовых операций по диапазону успешно выполнены `corepack pnpm --filter @tvoy-box/bot typecheck` и `corepack pnpm --filter @tvoy-box/bot build`.
- Внеплановая доработка (2026-05-14, запрос в рамках этапа 14): добавлен режим `Шаблон: дни + часы` в `Панель админа -> Даты и время`:
  - выбор дней недели;
  - выбор часов;
  - применение ко всему текущему горизонту (например, 14 дней) за один запуск.
- Решение (2026-05-14): шаблон работает по принципу «оставить открытыми только выбранные дни/часы», остальное автоматически закрывается (занятые слоты не трогаются).
- Доработка UX (2026-05-14): после применения шаблона бот показывает понятный итог:
  - какие дни/часы выбраны;
  - сколько слотов открыто/закрыто;
  - сколько осталось без изменений и сколько занятых слотов пропущено.
- Self-check (2026-05-14): после внедрения режима `Шаблон: дни + часы` успешно выполнены `corepack pnpm --filter @tvoy-box/bot typecheck` и `corepack pnpm --filter @tvoy-box/bot build`.
- Инцидент/фикс (2026-05-14): после внедрения шаблона в Telegram появились «кракозябры» в текстах и кнопках (`????`).
- Причина (2026-05-14): файл `apps/bot/src/handlers/navigation.ts` оказался сохранен в кодировке `Windows-1251` вместо `UTF-8`.
- Решение (2026-05-14): выполнена перекодировка `navigation.ts` обратно в `UTF-8` без изменения бизнес-логики; тексты интерфейса восстановлены.
- Self-check (2026-05-14): после фикса кодировки успешно выполнены `corepack pnpm --filter @tvoy-box/bot typecheck` и `corepack pnpm --filter @tvoy-box/bot build`.
- Допфикс (2026-05-14): в экране `Панель админа -> Даты и время` и связанных режимах (`Шаблон`, `Массово по диапазону`) удалены emoji-индикаторы и спец-стрелки, т.к. в части Telegram-клиентов они отображались как `??`.
- Решение (2026-05-14): для стабильности интерфейса установлены текстовые маркеры (`[OPEN]`, `[CLOSED]`, `[MIXED]`, `[BOOKED]`) и простая пагинация (`<` / `>`), чтобы исключить повторное искажение отображения.
- Self-check (2026-05-14): после допфикса отображения успешно выполнены `corepack pnpm --filter @tvoy-box/bot typecheck` и `corepack pnpm --filter @tvoy-box/bot build`.
- Допфикс UX (2026-05-14): по обратной связи пользователя в разделе `Панель админа -> Даты и время` убрана латиница и возвращены русские подписи во всех кнопках и служебных текстах.
- Решение (2026-05-14): индикация состояния дня перенесена в конец кнопки даты в формате `дата + open/total + статус в скобках` (например: `чт, 14.05 4/24 (частично)`), чтобы дата читалась первой и интерфейс был понятнее.
- Трудность (2026-05-14): временные ASCII-маркеры после предыдущего фикса (`[OPEN]/[MIXED]`) сохранились в UI и ухудшали читаемость.
- Self-check (2026-05-14): после возврата кириллицы и нового формата статусов успешно выполнены `corepack pnpm --filter @tvoy-box/bot typecheck` и `corepack pnpm --filter @tvoy-box/bot build`.
- Доработка UX (2026-05-14): по запросу пользователя убрана текстовая «сводка по датам» и словесные статусы `частично/есть занятые` из списка дат; оставлена только краткая легенда и кнопки дат.
- Решение (2026-05-14): для наглядности введены цветовые индикаторы:
  - в списке дат: `🟢` — в дате есть открытые часы, `⚪` — все часы закрыты;
  - в списке времени внутри даты: `🟢` открыто, `⚪` закрыто, `🔴` занято.
- Решение (2026-05-14): серверная логика слотов переведена на модель «по умолчанию закрыто»:
  - клиент видит только явно открытые слоты;
  - виртуальные часы в `trainer-grid` отдаются как `CLOSED`, чтобы тренер вручную открывал нужное время.
- Допфикс (2026-05-14): заменены оставшиеся «сломанные» символы (`??`, `?`) в кнопках пагинации/выбора на безопасные подписи (`<`, `>`, `•`, текст), чтобы интерфейс не распадался на разных Telegram-клиентах.
- Self-check (2026-05-14): после доработок успешно выполнены:
  - `corepack pnpm --filter @tvoy-box/api typecheck`;
  - `corepack pnpm --filter @tvoy-box/api build`;
  - `corepack pnpm --filter @tvoy-box/bot typecheck`;
  - `corepack pnpm --filter @tvoy-box/bot build`.
- Диагностика данных (2026-05-14): после перехода на `default-closed` в БД оставались исторические открытые слоты (`OPEN_IN_14D=215`), поэтому часть дат продолжала отображаться зеленой.
- Выполнено (2026-05-14): проведен разовый безопасный runtime-сброс открытых будущих слотов через `Prisma updateMany`:
  - обновлено `215` слотов из `OPEN` в `CLOSED`;
  - после сброса проверка показала `OPEN_IN_14D=0`, `BOOKED_IN_14D=0`.
- Решение (2026-05-14): в `bookings.service` изменено поведение после отмены/подтвержденного переноса — исходный `BOOKED`-слот теперь возвращается в `CLOSED` (а не `OPEN`), чтобы не нарушать модель «по умолчанию закрыто».
- Self-check (2026-05-14): после логического фикса и сброса данных повторно успешно выполнены:
  - `corepack pnpm --filter @tvoy-box/api typecheck`;
  - `corepack pnpm --filter @tvoy-box/api build`;
  - `corepack pnpm --filter @tvoy-box/bot typecheck`;
  - `corepack pnpm --filter @tvoy-box/bot build`.

---

## Этап 14. Деплой на сервер

Статус: `Завершен`

### Чек-лист текущего прогресса по этапу

- [x] Подготовить Docker-артефакты для изолированного запуска рядом с другими проектами на одном VPS.
- [x] Подготовить серверное окружение.
- [x] Развернуть проект.
- [x] Настроить webhook и HTTPS.
- [x] Настроить серверные логи и перезапуск.

### Логи по этапу

- Выполнено (2026-05-16): добавлены `deploy/compose.server.yml`, `infra/docker/Dockerfile.api`, `infra/docker/Dockerfile.bot`, `.env.server.example`, `scripts/deploy/deploy-server.sh`, `scripts/deploy/server-bootstrap.sh`, `docs/server-deploy.md`.
- Решение (2026-05-16): для каждого проекта на VPS используется свой `STACK_NAME`, отдельные compose-сети и тома; `PostgreSQL` не публикуется наружу, а API привязывается к `127.0.0.1` на отдельном порту, чтобы новые проекты не конфликтовали с уже запущенными контейнерами.
- Диагностика (2026-05-18): скрипт `scripts/deploy/install-codex-key.ps1` исправлен — теперь использует UTF-8, не показывает ложный успех и корректно завершает работу с ошибкой, если SSH-подключение не состоялось.
- Блокер (2026-05-18): парольный доступ к VPS со стороны агентского Windows OpenSSH требует отдельного TTY/интерактивного сценария; проверка серверного окружения еще не завершена.
- Диагностика (2026-05-18): ручная проверка `ssh root@62.113.111.4` на машине пользователя показала `WARNING: REMOTE HOST IDENTIFICATION HAS CHANGED!`; текущий блокер — устаревшая запись в `C:\Users\User\.ssh\known_hosts`, из-за которой OpenSSH не переходит к вводу пароля.
- Выполнено (2026-05-18): вместо нестабильного `ssh.exe` для парольного входа использован `paramiko`; это позволило автономно работать с VPS без ручного ввода пароля.
- Диагностика (2026-05-18): `Docker` на VPS не запускался из-за переполненного корневого раздела (`/dev/vda1` = `100%`); основными источниками были `17G` в `/opt/stack/backups` и старые слои `containerd`.
- Выполнено (2026-05-18): по явному разрешению пользователя удален старый `Supabase` со всеми файлами на сервере: `/opt/stack/supabase`, `/opt/stack/backups/supabase`, связанные контейнеры, образы и `supabase`-тома; после очистки на VPS освобождено около `15G`.
- Выполнено (2026-05-18): служба `docker` успешно запущена; на сервере сохранен существующий `n8n`, новый проект развернут рядом в отдельном каталоге `/opt/stack/tvoy-box-bot` и в отдельном compose-стеке `tvoy-box-bot`.
- Выполнено (2026-05-18): создан production-файл `.env.server` с отдельной серверной PostgreSQL, внутренним `API_BASE_URL=http://api:3000`, публикацией API только на `127.0.0.1:3300` и выравниванием `TRAINER_TELEGRAM_ID` под реальный `ADMIN_TELEGRAM_ID`.
- Выполнено (2026-05-18): в `infra/docker/Dockerfile.bot` добавлена генерация Prisma-клиента перед `pnpm build`; в `infra/docker/Dockerfile.api` и `infra/docker/Dockerfile.bot` runtime-пакетирование переведено на копирование уже собранного `/workspace`, чтобы убрать сбои `pnpm deploy` и missing `.prisma/client`.
- Self-check (2026-05-18): на VPS подтверждено рабочее состояние стека:
  - `tvoy-box-bot-postgres-1` — `healthy`;
  - `tvoy-box-bot-api-1` — `healthy`, `GET http://127.0.0.1:3300/health` возвращает `{"status":"ok", ... "database":{"status":"ok"}}`;
  - `tvoy-box-bot-bot-1` — `Up`, в логах есть `Telegram bot token validated` и `Bot polling started`.
- Диагностика (2026-05-18): после первого запуска админские разделы бота (`Заявки`, `Панель админа`) падали с `500`, хотя `/health` был зеленым; причина — в новой PostgreSQL не была применена Prisma-схема, поэтому отсутствовали таблицы `public.bookings` и `public.trainer_settings`.
- Выполнено (2026-05-18): в `deploy/compose.server.yml` добавлен одноразовый сервис `migrate`, а `scripts/deploy/deploy-server.sh` переведен на последовательность `postgres -> build -> prisma db push -> api+bot`; это делает серверный деплой повторяемым и не зависит от ручного захода в контейнер.
- Выполнено (2026-05-18): `scripts/deploy/deploy-server.sh` перестал `source`-ить `.env.server` целиком, потому что `GOOGLE_PRIVATE_KEY` ломал bash-разбор; нужные переменные для статуса теперь читаются точечно через `grep`.
- Self-check (2026-05-18): на VPS вручную подтверждено, что после `docker compose run --rm migrate` и перезапуска контейнеров проблемные ручки больше не падают:
  - `GET /bookings/pending?trainerTelegramId=492732093` -> `200 {"status":"ok","items":[]}`;
  - `GET /trainer-settings/current?trainerTelegramId=492732093` -> `200 {"status":"ok","settings":...}`.
- Self-check (2026-05-18): после финального перезапуска на VPS активны актуальные контейнеры:
  - `tvoy-box-bot-api-1` — пересоздан и снова `healthy`;
  - `tvoy-box-bot-bot-1` — пересоздан, повторно прошел `Telegram bot token validated` и `Bot polling started`.
- Ручная проверка пользователя (2026-05-18): подтверждено, что тренерское меню в Telegram снова открывается и работает после серверного деплоя и `Prisma db push`.
- Решение (2026-05-18): этап webhook/HTTPS пока сознательно не выполнялся, потому что текущая production-модель бота использует polling и не требует внешнего webhook для запуска; при переходе на webhook нужно будет отдельно встроиться в уже работающий на VPS `Caddy`.

---

## Этап 15. Сквозная проверка MVP

Статус: `Завершен`

### Чек-лист текущего прогресса по этапу

- [x] Пройти полную цепочку клиента.
- [x] Пройти полную цепочку админа.
- [x] Сверить поведение с ТЗ.

### Логи по этапу

- Диагностика (2026-05-15): `corepack pnpm typecheck` успешно пройден по всему workspace.
- Диагностика (2026-05-15): обнаружен build-блокер в `apps/mini-app` — `Next.js` не собирался из-за отсутствия `root layout`; дополнительно `root build` падал на конфигурации `tsconfig` и нестабильном параллельном запуске `pnpm -r build`.
- Выполнено (2026-05-15): в `apps/mini-app` добавлены `src/app/layout.tsx`, `src/app/globals.css`, обновлен `src/app/page.tsx`; заглушка mini app приведена к аккуратному рабочему виду и больше не ломает сборку.
- Выполнено (2026-05-15): конфигурация `apps/mini-app/tsconfig.json` приведена в совместимое состояние с `Next.js App Router`, а корневой `build`-скрипт переведен на последовательный запуск (`--workspace-concurrency=1`) для стабильной сборки monorepo.
- Self-check (2026-05-15): успешно выполнены `corepack pnpm --filter @tvoy-box/mini-app typecheck`, `corepack pnpm --filter @tvoy-box/mini-app build`, `corepack pnpm --filter @tvoy-box/bot typecheck`, `corepack pnpm --filter @tvoy-box/bot build`, `corepack pnpm typecheck`, `corepack pnpm build`.
- Доработка UX (2026-05-15): в `apps/bot/src/handlers/navigation.ts` для раздела `Панель админа -> Даты и время` убраны emoji-маркеры статусов слотов в пользу безопасных текстовых меток (`[откр]`, `[закр]`, `[зан]`); также восстановлен единый заголовок `Выберите раздел ↓`.
- Доработка UX (2026-05-15): в `apps/bot/src/services/screen-service.ts` обновлены устаревшие тексты экранов, чтобы fallback-описания соответствовали уже реализованным сценариям клиента и тренера.
- Блокер (2026-05-15): `corepack pnpm db:runtime-check` завершился ошибкой `Can't reach database server at localhost:5432`; `watchdog-status` показывает, что `watchdog`, `api` и `bot` сейчас не запущены.
- Вывод (2026-05-15): кодовые и интерфейсные доработки для старта Этапа 15 выполнены, но полный сквозной прогон клиента и админа можно продолжить только после подъема локальной PostgreSQL и live-процессов `api + bot`.
- Выполнено (2026-05-18): проведен production smoke-test на VPS через живой API `127.0.0.1:3300` с автоматической очисткой тестовых данных по сценарию `register client -> open slot -> available slots -> booking request -> pending list -> booking details -> confirm -> client trainings -> cancel-training`.
- Self-check (2026-05-18): smoke-test завершился успешно; боевой endpoint `POST /bookings/confirm` больше не падает `500`, а `GET /health` после пересборки API на VPS вернул `200 {"status":"ok","database":{"status":"ok"}}`.
- Решение (2026-05-18): сквозная проверка этапа выполнена на production-стеке, потому что именно там воспроизводился runtime-блокер подтверждения записи; тестовые сущности (`telegramId=9000000015`, временный слот, booking/training/calendarSyncLog`) после прогона были автоматически удалены.

---

## Этап 16. Постзапусковая стабилизация

Статус: `Завершен`

### Чек-лист текущего прогресса по этапу

- [x] Анализировать реальные ошибки по логам.
- [x] Исправлять и перепроверять проблемные сценарии.
- [x] Восстановить работу `api + bot` после инцидента «бот не реагирует».
- [x] Очистить тестовые заявки из БД по маркерам `stage*` / `9000000000*`.
- [x] Убрать ограничение отображения заявок до 8 в админском списке.
- [x] Устранить кракозябры в пользовательских PowerShell-скриптах.
- [x] Убрать зависимость single-trainer режима от placeholder `TRAINER_TELEGRAM_ID`.
- [x] Добавить preflight-проверку Google Calendar credentials и инструкцию безопасной замены ключа.
- [x] Заменить поврежденный `GOOGLE_PRIVATE_KEY` на валидный ключ service account и перевести production на file-based credentials, чтобы вернуть реальную синхронизацию с Google Calendar.
- [x] Устранить системную проблему `watchdog` (`Path/PATH duplicate key`) для стабильного автозапуска.
- [x] Перевести локальный live-start на Docker PostgreSQL и снять системный блокер первого запуска Docker Desktop на Windows.
- [x] Добавить user-facing команду `dev:live:start`, которая одной командой поднимает локальную БД и сервисы.

### Логи по этапу

- Диагностика (2026-05-14): `watchdog-status` показал, что `watchdog/api/bot` не запущены; `http://localhost:3000/health` недоступен.
- Диагностика (2026-05-14): локальная БД не принимала подключения (`5432`), после очистки stale `postmaster.pid` и запуска portable PostgreSQL восстановлена доступность `Prisma runtime-check: OK`.
- Диагностика (2026-05-14): `watchdog` падал на системной ошибке окружения Windows (`Path/PATH` duplicate key), поэтому автозапуск не поднимал процессы.
- Выполнено (2026-05-14): сервисы `api` и `bot` запущены вручную вне sandbox; подтверждено по логам:
  - API: успешный старт Nest и `GET /health = ok`;
  - Bot: `Telegram bot token validated`, `Bot polling started`, получено входящее сообщение `start`.
- Решение (2026-05-14): для текущего окружения использовать ручной live-запуск при падении watchdog; отдельно запланировать фикс watchdog-скрипта по конфликту переменных `Path/PATH`.
- Выполнено (2026-05-14): в `apps/bot/src/handlers/navigation.ts` убран лимит `slice(0, 8)` для текста и кнопок в разделе `admin-requests`; строка `... и еще N заявок` удалена.
- Выполнено (2026-05-14): из локальной БД удалены тестовые данные по явным маркерам (`username startsWith stage*`, `fullName startsWith Stage *`, `telegramId startsWith 9000000000*`).
- Runtime-check (2026-05-14): фактически удалено:
  - `clientsDeleted=7`;
  - `bookings=6`;
  - `trainings=2`;
  - `calendarSyncLog=4`;
  - `slotsDeleted=5`.
- Self-check (2026-05-14): после правок списка заявок успешно выполнены `corepack pnpm --filter @tvoy-box/bot typecheck` и `corepack pnpm --filter @tvoy-box/bot build`.
- Диагностика (2026-05-14): по `logs/bot/runtime.jsonl` подтверждена первопричина текущего сбоя `/start` — `400: Bad Request: text must be non-empty`.
- Выполнено (2026-05-14): в `apps/bot/src/handlers/start.ts` и `apps/bot/src/handlers/navigation.ts` для админ-экрана установлен явный текст `Выберите раздел ↓` вместо невидимых символов.
  - Self-check (2026-05-14): после runtime-фикса `/start` успешно выполнены `corepack pnpm --filter @tvoy-box/bot typecheck` и `corepack pnpm --filter @tvoy-box/bot build`.
  - Диагностика (2026-05-14): после правки кода в `runtime.jsonl` больше не появляется новая ошибка `text must be non-empty`, но автозапуск через `watchdog` в текущем окружении нестабилен, из-за чего пользователь мог видеть поведение старого процесса.
  - Выполнено (2026-05-14): в `scripts/dev/watchdog-loop.ps1` запуск `api/bot` переведен с `Start-Process` на `.NET ProcessStartInfo` для обхода системной ошибки Windows `Path/PATH duplicate key`.
  - Статус (2026-05-14): фиксация по `watchdog` частичная, требуется дополнительная проверка стабильного фонового запуска на живом окружении пользователя.
- Диагностика (2026-05-18): в локальном `.env` `TRAINER_TELEGRAM_ID` оставался тестовой заглушкой `123456789`, из-за чего уведомления тренеру уходили в несуществующий чат `400: chat not found`, хотя по ТЗ у нас один тренер и один администратор.
- Выполнено (2026-05-18): в `apps/bot/src/config/bot-config.ts` и `apps/api/src/config/app-config.service.ts` добавлен fallback `TRAINER_TELEGRAM_ID -> ADMIN_TELEGRAM_ID`, если значение отсутствует или осталось placeholder-ом; `.env.example` приведен к безопасному шаблону без хардкода `123456789`.
- Выполнено (2026-05-18): `scripts/dev/watchdog-*.ps1` и `scripts/deploy/install-codex-key.ps1` переведены на единый UTF-8-сценарий (`InputEncoding/OutputEncoding`, `chcp 65001`, сохранение с BOM), чтобы обычный Windows PowerShell не показывал кракозябры в подсказках и статусах.
- Выполнено (2026-05-18): дополнительно вычищены битые UTF-8 строки в `apps/bot/src/handlers/navigation.ts` для админского блока закрытия периодов и диагностических логов переходов, а также восстановлены русские комментарии в `.env.server.example`; это убирает смешение нормального текста и «кракозябр» в текущем рабочем сценарии деплоя/админки.
- Выполнено (2026-05-18): `watchdog-status.ps1` и `watchdog-stop.ps1` теперь учитывают stale `postmaster.pid` и не показывают «живой» PostgreSQL по одному только старому PID-файлу.
- Диагностика (2026-05-18): прямой запуск `.tools/postgres/dist/pgsql/bin/postgres.exe -D .tools/postgres/data` в текущем Windows-окружении завершается сразу, а `localhost:5432` не поднимается; это подтверждает, что текущий блокер локального live-start связан с portable PostgreSQL, а не с кодом `api`/`bot`.
- Self-check (2026-05-18): успешно выполнен `corepack pnpm build`; fallback конфигурации Telegram подтвержден через загрузку собранных `dist`-конфигов, `watchdog-status.ps1` теперь возвращает корректный `postgres.pid = null` вместо stale значения.
- Диагностика (2026-05-18): production smoke-test на VPS выявил реальный серверный блокер `POST /bookings/confirm -> 500` в `GoogleCalendarService.buildSignedJwt`; приватный ключ из `GOOGLE_PRIVATE_KEY` не десериализуется ни в Node.js, ни в Python (`ERR_OSSL_UNSUPPORTED` / ASN.1 parsing error), значит проблема в текущем значении секрета, а не в Docker.
- Выполнено (2026-05-18): в `apps/api/src/modules/bookings/bookings.service.ts` подтверждение записи, клиентский перенос и принятие предложенного времени переведены на degradation-safe режим: бизнес-операция завершается даже при сбое Google Calendar, а в `calendarSyncLog` пишется `FAILED` вместо падения `500`.
- Выполнено (2026-05-18): пересобран production API на VPS; повторный smoke-test `register -> request -> confirm -> cancel` пройден полностью, а `scripts/deploy/deploy-server.sh` дополнительно очищен от битой кодировки, чтобы серверный деплой больше не печатал кракозябры.
- Выполнено (2026-05-18): добавлен отдельный preflight-чек `corepack pnpm qa:google-calendar-creds .env.server` и переписан `docs/server-deploy.md` с явным разделом по замене `GOOGLE_PRIVATE_KEY`, чтобы календарный инцидент диагностировался до деплоя, а не во время `POST /bookings/confirm`.
- Self-check (2026-05-18): новая проверка credentials отработала на текущем `.env.server` с ожидаемым `FAILED / ERR_OSSL_UNSUPPORTED`, тем самым подтверждая, что следующим реальным шагом остается именно замена service-account private key, а не доработка кода.
- Выполнено (2026-05-18): production-конфигурация Google Calendar переведена с inline `GOOGLE_PRIVATE_KEY` в `.env.server` на file-based credentials через `.secrets/google-service-account.json` + переменные `GOOGLE_SERVICE_ACCOUNT_JSON_SOURCE` / `GOOGLE_SERVICE_ACCOUNT_JSON_PATH`; это убирает хрупкость ручного экранирования PEM и делает секрет повторно используемым для новых деплоев без правки многострочного ключа в `.env`.
- Выполнено (2026-05-18): `apps/api/src/modules/google-calendar/google-calendar.service.ts` доработан так, чтобы API сначала читал credentials из JSON-файла service account, а только потом использовал старые `GOOGLE_SERVICE_ACCOUNT_EMAIL` / `GOOGLE_PRIVATE_KEY` как fallback; `deploy/compose.server.yml` получил read-only mount секрета в контейнер API.
- Выполнено (2026-05-18): локально создан `.secrets/google-service-account.json`, `.gitignore` и `.dockerignore` дополнены исключением `.secrets/`, а `scripts/qa/google-calendar-credentials-check.mjs` расширен проверкой file-based credentials.
- Self-check (2026-05-18): успешно выполнены `corepack pnpm --filter @tvoy-box/api typecheck`, `corepack pnpm --filter @tvoy-box/api build`, `corepack pnpm qa:google-calendar-creds .env.server`; новая preflight-проверка завершилась успехом и подтвердила, что JSON-ключ корректно подписывает JWT.
- Выполнено (2026-05-18): production-стек на VPS пересобран и перезапущен с новым JSON-ключом service account; `GET http://127.0.0.1:3300/health` после деплоя вернул `200`.
- Self-check (2026-05-18): проведен повторный production smoke-test на VPS по живому сценарию `register -> open slot -> request -> confirm -> cancel -> cleanup`; в `calendar_sync_logs` зафиксированы:
  - `CREATE / SUCCESS` с сообщением `Google Calendar event created on booking confirmation`;
  - `CANCEL / SUCCESS` с сообщением `Google Calendar event cancelled on training cancellation`.
- Решение (2026-05-21): незавершенный локальный Windows-блокер portable PostgreSQL больше не считается открытым хвостом этого этапа. Он перенесен в зону `Этапа 24`, потому что относится уже к локальному dev-runtime mini app, а не к постзапусковой стабилизации основного production-бота.
- Выполнено (2026-05-19): в DNS-зоне `anyatobolova.ru` добавлены поддомены `api.anyatobolova.ru` и `app.anyatobolova.ru`, оба указывают на VPS `62.113.111.4`.
- Диагностика (2026-05-19): на VPS уже работал системный `Caddy` на портах `80/443` для `n8n.anyatobolova.ru`; вместо запуска второго reverse proxy в Docker принято решение встроить новые поддомены в существующий host-level `Caddy`, чтобы не создавать конфликт по портам и упростить будущий mini app.
- Выполнено (2026-05-19): production-бот переведен на webhook-режим:
  - в `apps/bot` добавлены `BOT_DELIVERY_MODE`, `BOT_WEBHOOK_*`, `BOT_PORT` и локальный HTTP webhook-listener;
  - `deploy/compose.server.yml` теперь публикует bot webhook только на `127.0.0.1:3301`, а API, как и раньше, на `127.0.0.1:3300`;
  - системный `/etc/caddy/Caddyfile` на VPS проксирует `https://api.anyatobolova.ru/telegram/webhook/...` на `127.0.0.1:3301`, все остальные пути `api.anyatobolova.ru` — на `127.0.0.1:3300`, а `app.anyatobolova.ru` пока зарезервирован заглушкой под будущий mini app.
- Выполнено (2026-05-19): `Caddy` успешно выпустил Let's Encrypt сертификаты для `api.anyatobolova.ru` и `app.anyatobolova.ru`; публичные HTTPS-проверки отдали `HTTP/2 200`.
- Диагностика (2026-05-19): первый запуск webhook-бота падал на `setWebhook` с `400 Bad Request: bad webhook: Failed to resolve host`, потому что Telegram ещё не успел увидеть свежую DNS-запись.
- Решение (2026-05-19): в `apps/bot/src/main.ts` добавлен retry-цикл регистрации webhook по той же модели, что и у token validation. Это делает production устойчивым к временной задержке DNS/Telegram после смены домена и избавляет от ручных перезапусков.
- Self-check (2026-05-19): после повторного деплоя на VPS подтверждено:
  - `curl http://127.0.0.1:3300/health` -> `200`;
  - `curl -I https://api.anyatobolova.ru/health` -> `HTTP/2 200`;
  - `curl -I https://app.anyatobolova.ru/` -> `HTTP/2 200`;
  - `docker ps` показывает `tvoy-box-bot-bot-1` c `127.0.0.1:3301->8081/tcp`;
  - `getWebhookInfo` Telegram возвращает реальный URL `https://api.anyatobolova.ru/telegram/webhook/...` и `ip_address=62.113.111.4`.
- Выполнено (2026-05-19): изменения зафиксированы в GitHub коммитом `e12a81b feat: stabilize local docker dev and production webhook` и отправлены в `origin/main`.
- Диагностика (2026-05-19): каталог `/opt/stack/tvoy-box-bot` на VPS не является `git clone` (внутри нет `.git`), поэтому обычный `git pull` на сервере недоступен. Актуальный production-состояние синхронизировано прямой загрузкой обновленных файлов и повторным `bash scripts/deploy/deploy-server.sh`.
- Решение (2026-05-18): вместо "автообновления" service-account key выбрана более надежная схема хранения полного JSON-файла секрета и обязательный preflight перед деплоем. Ключи Google не ротируются сами по себе; для их плановой замены достаточно обновить `.secrets/google-service-account.json`, прогнать `corepack pnpm qa:google-calendar-creds .env.server` и повторить `bash scripts/deploy/deploy-server.sh`.
- Выполнено (2026-05-18): `watchdog`-скрипты дополнительно переработаны для Windows-окружения:
  - добавлен foreground-раннер `scripts/dev/postgres-runner.ps1`;
  - в `package.json` добавлена команда `corepack pnpm dev:postgres:run`;
  - `watchdog-start` теперь завершаетcя понятной подсказкой, если portable PostgreSQL не удается поднять в фоне.
- Self-check (2026-05-18): `corepack pnpm dev:watchdog:start` больше не падает на системной ошибке `Path/PATH duplicate key`; вместо этого воспроизводимо выдает диагностируемую ошибку только по фоновому старту portable PostgreSQL и рекомендует явный обход `corepack pnpm dev:postgres:run`.
- Self-check (2026-05-18): прямой foreground-запуск `postgres.exe -D .tools/postgres/data` из текущего окружения доходит до сообщения `система БД готова принимать подключения`, то есть код `api/bot` и сама база не сломаны; остаточный блокер — именно фоновый hidden/background launch portable PostgreSQL на этой Windows-машине.
- Диагностика (2026-05-18): локально `docker` не установлен, поэтому автоматический обход через dev-PostgreSQL в Docker в текущем окружении недоступен.
- Решение (2026-05-18): до установки нормального локального PostgreSQL или Docker повторяемый Windows-сценарий фиксируется как `1) открыть отдельное окно PowerShell -> 2) запустить corepack pnpm dev:postgres:run -> 3) в рабочем окне выполнить corepack pnpm dev:watchdog:start`; это лучше, чем прежний немой сбой `watchdog`, и прозрачно отделяет внешний инфраструктурный блокер от кода проекта.
- Выполнено (2026-05-18): автоматический запуск PowerShell-команд прогнан повторно без участия пользователя (`dev:watchdog:stop`, попытка отдельного PowerShell-процесса для `postgres-runner`, `pg_isready`, `dev:watchdog:start`, `dev:watchdog:status`, `db:runtime-check`).
- Self-check (2026-05-18): подтверждено, что после всех правок проблема локализована окончательно:
  - `watchdog` больше не падает по `Path/PATH`;
  - `watchdog-start` теперь всегда отдает понятную ошибку про фоновый старт PostgreSQL;
  - `pg_isready` после автоматического отдельного PowerShell-процесса по-прежнему возвращает `localhost:5432 - нет ответа`.
- Вывод (2026-05-18): на этой машине следующий реальный шаг уже не в коде проекта, а в замене способа локального запуска БД (установка системного PostgreSQL или Docker Desktop). До этого момента локальный fully-automatic live-start через bundled portable PostgreSQL остается внешним Windows-блокером.
- Выполнено (2026-05-18): начат перевод локальной БД на Docker-схему:
  - в `package.json` добавлены команды `corepack pnpm dev:db:up`, `dev:db:down`, `dev:db:status`;
  - добавлены PowerShell-обертки `scripts/dev/db-up.ps1`, `db-down.ps1`, `db-status.ps1`;
  - `watchdog-start/loop/status/stop` доработаны так, чтобы при наличии Docker предпочитать `docker compose` для `postgres`, а portable PostgreSQL оставлять только fallback-веткой.
- Выполнено (2026-05-18): dev-скрипты переведены на отдельный project-local `DOCKER_CONFIG=.docker-dev`, чтобы локальная БД не зависела от битого или недоступного пользовательского `C:\Users\User\.docker\config.json`.
- Выполнено (2026-05-18): `Docker Desktop 4.73.0` установлен через `winget`; CLI появился в `C:\Program Files\Docker\Docker\resources\bin\docker.exe`.
- Диагностика (2026-05-18): после установки Docker Desktop выявлен системный блокер первого старта:
  - из текущего служебного sandbox-сеанса Docker pipes отдают `Access is denied`, потому что процесс идет не под обычным пользователем `User`, а под отдельной служебной учеткой;
  - even в elevated-проверке `docker version` серверная часть возвращает `500 Internal Server Error`;
  - системная служба `com.docker.service` остается в `Stopped`, а из текущего автоматизированного сеанса не стартует принудительно.
- Диагностика (2026-05-18): локальная группа `docker-users` уже содержит пользователя `User`, то есть конфигурация доступа для обычного пользовательского сеанса в целом подготовлена; проблема остается именно в текущем служебном/установочном состоянии после свежей установки Docker Desktop.
- Решение (2026-05-18): проектный переход на Docker для локальной БД считается подготовленным на уровне кода и скриптов; следующий одноразовый системный шаг — обычный вход пользователя в обновленный Windows-сеанс после установки Docker Desktop, чтобы `com.docker.service`/docker pipes поднялись уже в нормальном пользовательском токене.
- Выполнено (2026-05-19): добавлен общий helper `scripts/dev/docker-helpers.ps1`; локальные команды `dev:db:*` и `watchdog:*` переведены на единый preflight Docker daemon status вместо прямого падения на `permission denied`/pipe errors.
- Self-check (2026-05-19): `corepack pnpm dev:watchdog:stop` теперь завершается штатно даже при недоступном Docker daemon; `corepack pnpm dev:watchdog:status` возвращает корректный JSON со всеми сервисами в `running=false` вместо stacktrace.
- Self-check (2026-05-19): `corepack pnpm dev:db:status` теперь отдает явную диагностическую причину `Docker Desktop is installed, but the daemon is not reachable from the current session...` вместо аварийного PowerShell-исключения.
- Диагностика (2026-05-19): `com.docker.backend` и окно Docker Desktop запущены, но backend логирует `backend is not running`; `docker version` из служебной сессии стабильно упирается в `permission denied while trying to connect to the docker API at npipe:////./pipe/docker_engine`.
- Диагностика (2026-05-19): `wsl.exe --status/--version` до системной правки не возвращали нормальный статус и показывали help/stub output, что указывало на недопримененные WSL prerequisites для Docker Desktop.
- Выполнено (2026-05-19): успешно выполнена системная команда `wsl.exe --install --no-distribution`; Windows подтвердила установку компонента `Подсистема Windows для Linux` и потребовала еще одну перезагрузку для применения изменений.
- Решение (2026-05-19): кодовый и скриптовый слой локального Docker-режима считаем доведенным; следующий ожидаемый шаг — повторная перезагрузка Windows, после которой нужно заново проверить `corepack pnpm dev:db:up`, `dev:db:status`, `db:runtime-check` и `dev:watchdog:start`.
- Диагностика (2026-05-19, после второй перезагрузки): `wsl.exe --status` уже показывает `версия по умолчанию: 2`, а безопасный цикл `wsl --shutdown -> restart Docker Desktop` восстановил живой Docker engine (`docker version` снова вернул полноценный `Server: Docker Desktop 4.73.1`).
- Выполнено (2026-05-19): локальный `postgres` реально поднят в Docker через `docker compose up -d postgres`; контейнер `tvoy-box-local-postgres-1` находится в состоянии `Up`, порт `5432` опубликован на `localhost`.
- Self-check (2026-05-19): `corepack pnpm db:runtime-check` завершился успешно с `Prisma runtime-check: OK`, значит локальная БД через Docker доступна проекту и `DATABASE_URL` больше не блокирует live-режим.
- Диагностика (2026-05-19): `corepack pnpm dev:watchdog:start` из служебной сессии Codex доходит до сообщения `PostgreSQL already accepting connections` и создает `manager.pid/state.json`, но скрытый фоновый PowerShell-менеджер не удерживается после завершения автоматизированной команды.
- Self-check (2026-05-19): прямой запуск `powershell -File scripts/dev/watchdog-loop.ps1` в foreground не упал сам по себе и был остановлен только по timeout среды, что подтверждает: цикл `watchdog-loop` как код не разваливается; остаточный блокер относится к удержанию hidden background processes именно в служебной sandbox-сессии, а не к логике проекта.
- Вывод (2026-05-19): проектный локальный Docker-сценарий стабилизирован до рабочего уровня по БД и preflight-проверкам. Для обычной пользовательской PowerShell-сессии следующий практический сценарий выглядит как `Docker Desktop открыт -> corepack pnpm dev:db:up -> corepack pnpm db:runtime-check -> corepack pnpm dev:watchdog:start`; в автоматизированной служебной сессии Codex последний шаг ограничен политикой удержания фоновых процессов.
- Выполнено (2026-05-19): добавлены user-facing команды `corepack pnpm dev:live:start`, `dev:live:stop`, `dev:live:status` и новые скрипты `scripts/dev/live-*.ps1`; теперь локальный стек можно поднять одной командой без ручного вызова внутренних `db/watchdog`-скриптов.
- Решение (2026-05-19): `dev:live:start` сделан foreground-оркестратором, а не еще одним hidden-manager'ом. Это намеренно обходит ограничение служебной sandbox-сессии на удержание фоновых процессов и делает поведение понятным для обычного пользователя: команда держит окно открытым, а `Ctrl+C` останавливает API, бота и локальный PostgreSQL.
- Решение (2026-05-19): в `dev:live:start` локальный бот по умолчанию стартует с `BOT_DRY_RUN=true`, чтобы не конфликтовать с уже работающим production polling на VPS (`409 Conflict`). Для реального локального polling нужен отдельный осознанный запуск с флагом `-EnableTelegramPolling` после остановки другого экземпляра бота.
- Self-check (2026-05-19): тестовый прогон `scripts/dev/live-start.ps1 -MaxRuntimeSeconds 12` подтвердил рабочую последовательность user-facing сценария:
  - Docker PostgreSQL поднимается;
  - API доходит до успешного старта и отвечает на health в процессе прогона;
  - локальный бот в safe-режиме больше не упирается в `409 Conflict` от production polling;
  - по завершении тестового тайм-лимита временный live-стек корректно снимается.

---

## Этап 17. Автодеплой с GitHub на VPS

Статус: `Завершен (GitHub Secrets заведены, workflow в GitHub Actions успешно отработал, production deploy после push в `main` подтвержден)`

### Чек-лист текущего прогресса по этапу

- [x] Выбрать и зафиксировать безопасную схему автодеплоя без ручного входа на VPS после каждого `push`.
- [x] Добавить GitHub Actions workflow для production deploy после `push` в `main`.
- [x] Подготовить серверный remote-deploy сценарий с releases/shared/current и атомарным переключением.
- [x] Подготовить VPS под автодеплой: отдельный `deploy`-пользователь, SSH-ключ, каталоги `releases/shared`.
- [x] Подготовить понятную инструкцию по ручной настройке GitHub Secrets.
- [x] Выполнить self-check структуры и зафиксировать, что останется сделать вручную.
- [x] Выполнить первый реальный запуск workflow из GitHub Actions и подтвердить автоматическое обновление production на VPS.

### Логи по этапу

- Решение (2026-05-19): для автодеплоя выбрана схема `GitHub Actions -> SSH -> upload release archive -> remote deploy`, а не `git pull` на VPS. Причина: текущий production-каталог `/opt/stack/tvoy-box-bot` не является `git clone`, а upload release-архива не требует хранить GitHub PAT/Deploy Key на сервере.
- Решение (2026-05-19): секреты production (`.env.server`, `.secrets/google-service-account.json`) должны жить в отдельном `shared`-каталоге на VPS и не зависеть от содержимого конкретного release.
- Выполнено (2026-05-19): добавлены файлы автодеплоя:
  - `.github/workflows/deploy-production.yml` — workflow после `push` в `main`;
  - `scripts/deploy/remote-deploy.sh` — release/deploy сценарий на VPS;
  - `scripts/deploy/setup-server-autodeploy.sh` — одноразовая подготовка VPS;
  - `docs/auto-deploy.md` — пошаговая инструкция по GitHub Secrets и проверке.
- Выполнено (2026-05-19): локально сгенерирована отдельная ключевая пара для GitHub Actions и host record для `known_hosts`:
  - `.secrets/github-actions/vps_deploy_ed25519`
  - `.secrets/github-actions/vps_deploy_ed25519.pub`
  - `.secrets/github-actions/vps_known_hosts.txt`
- Выполнено (2026-05-19): на VPS создан отдельный пользователь `deploy`, добавлен в группу `docker`, ему выдан SSH-доступ по новой ключевой паре; подготовлен новый deploy-root `/opt/stack/tvoy-box-bot-deploy` с каталогами `releases/` и `shared/`.
- Выполнено (2026-05-19): из legacy-каталога `/opt/stack/tvoy-box-bot` в новый `shared`-каталог скопированы production `.env.server`, `.secrets/google-service-account.json` и логи, чтобы автодеплой не зависел от рабочего дерева release.
- Диагностика (2026-05-19): первая end-to-end проверка `remote-deploy.sh` упала на `set: pipefail\r: invalid option name` — release на сервере унаследовал `CRLF` в `deploy-server.sh`.
- Решение (2026-05-19): добавлен `.gitattributes` с `LF` для `*.sh/*.yml/*.yaml/Dockerfile*`, а `remote-deploy.sh` теперь принудительно нормализует line endings для shell-скриптов после распаковки release.
- Self-check (2026-05-19): полный прогон новой схемы уже подтвержден вручную вне GitHub:
  - вход `deploy` по SSH-ключу работает;
  - `deploy` видит Docker и может выполнять `docker ps`;
  - `remote-deploy.sh` успешно распаковал release в `/opt/stack/tvoy-box-bot-deploy/releases/verify-*`;
  - symlink `current` переключается на новый release;
  - `bash scripts/deploy/deploy-server.sh` отрабатывает из нового deploy-root;
  - `curl http://127.0.0.1:3300/health` после такого деплоя возвращает `200`.
- Выполнено (2026-05-19): в GitHub репозитории добавлены Secrets `VPS_HOST`, `VPS_PORT`, `VPS_USER`, `VPS_SSH_PRIVATE_KEY`, `VPS_KNOWN_HOSTS`.
- Self-check (2026-05-19): первый реальный GitHub Actions запуск `Deploy Production` стартовал автоматически после push коммита `9245fa4` и завершился успешно (`Deploy to VPS succeeded`), что подтвердило рабочую цепочку `push -> upload release archive -> remote deploy -> healthcheck`.
- Диагностика (2026-05-19): в финале успешного workflow GitHub показал предупреждение о будущем снятии Node.js 20 на раннерах для `actions/checkout@v4` и `webfactory/ssh-agent@v0.9.0`.
- Решение (2026-05-19): предупреждение не блокирует текущий деплой и не считается инцидентом production; его нужно закрыть отдельной сервисной задачей перед 2026-09-16 обновлением action-раннеров GitHub.
- [ ] До 2026-09-16 обновить GitHub Actions в `.github/workflows/deploy-production.yml` до версий, совместимых с Node.js 24, и повторно прогнать production workflow.
  Лог/пояснение: задача добавлена после первого успешного автодеплоя, потому что GitHub уже предупреждает о будущем снятии Node.js 20 на раннерах; сейчас это не блокер, но обновление нужно выполнить заранее, а не в день дедлайна.

---

## Этап 18. Разделение веток dev и main

Статус: `Завершен`

### Чек-лист текущего прогресса по этапу

- [x] Зафиксировать правило: `main` используется только как production-ветка, `dev` — только как ветка разработки и тестирования.
- [x] Проверить, что production workflow по `push` уже слушает только `main`.
- [x] Добавить дополнительную защиту, чтобы ручной `workflow_dispatch` тоже не деплоил код не из `main`.
- [x] Создать ветку `dev` из текущего состояния `main`.
- [x] Опубликовать ветку `dev` в GitHub.
- [x] Зафиксировать итоговую схему работы с ветками в логе этапа.

### Логи по этапу

- Решение (2026-05-19): принимается простая схема веток без промежуточных release-веток: `dev` служит для разработки и проверок, `main` — единственный источник production-деплоя на VPS.
- Диагностика (2026-05-19): до правки workflow уже автоматически деплоил только `push` в `main`, но ручной `workflow_dispatch` теоретически можно было запустить из другой ветки.
- Выполнено (2026-05-19): в `.github/workflows/deploy-production.yml` добавлен job-level guard `if: github.ref == 'refs/heads/main'`, чтобы даже ручной запуск workflow из `dev` не отправлял код на сервер.
- Выполнено (2026-05-19): ветка `dev` создана из актуального состояния `main` и опубликована в GitHub как отдельная ветка для разработки.
- Итог (2026-05-19): рабочее правило проекта теперь такое:
  - вся разработка и промежуточные проверки идут в `dev`;
  - в `main` попадает только то, что готово к production;
  - автодеплой на VPS реагирует только на `main`, а ручной запуск workflow из `dev` не деплоит код на сервер.

---

## Этап 19. Согласование концепции mini app

Статус: `Завершен`

### Чек-лист текущего прогресса по этапу

- [x] Зафиксировать, что mini app — это дополнение к работающему боту, а не замена backend-логики и не отдельный продукт вне Telegram.
- [x] Определить визуальное направление mini app на основе референса школы `Твой Бокс`.
- [x] Согласовать клиентский контур mini app: запись, мои записи, перенос, отмена, запрос без слота, профиль.
- [x] Согласовать тренерский контур mini app: заявки, будущие тренировки, управление слотами, настройки записи, черный список, поиск клиентов, выгрузка.
- [x] Зафиксировать модель уведомлений первой версии: часть системных уведомлений остается в боте, внутри mini app показывается актуальный статус.
- [x] Согласовать базовый состав профиля клиента и принцип первичного заполнения данных из Telegram.
- [x] Согласовать поэтапную разработку mini app без риска для production-бота.
- [x] Подготовить детальную карту экранов, состояний и переходов как финальную спецификацию перед началом реализации.

### Логи по этапу

- Решение (2026-05-19): mini app становится основным удобным интерфейсом внутри Telegram для клиента и тренера, а текущий бот сохраняется как резервный сценарий и как канал части уведомлений первой версии.
- Решение (2026-05-19): визуальный стиль mini app должен быть дружелюбным и соответствовать философии школы; в качестве референса принят текущий сайт школы, палитра — светлая, с опорой на фирменные `красный / черный / белый`, без темной темы.
- Решение (2026-05-19): в интерфейсе используются логотип школы, приветственный блок и фото тренера; навигация строится через карточки и короткие понятные действия без перегрузки экрана.
- Решение (2026-05-19): на первом клиентском экране нужен приветственный блок с логотипом и фото, карточка `Записаться`, карточка `Мои записи`; `Профиль` и `Связь/помощь` выносятся в верхнее меню mini app, а не в отдельные крупные карточки главного экрана.
- Решение (2026-05-19): клиентский путь в mini app должен быть полным уже в первой версии — без обязательного возврата в бот при штатной работе mini app.
- Решение (2026-05-19): первичная регистрация упрощается — данные по возможности подтягиваются из Telegram, а клиент при необходимости дозаполняет профиль внутри mini app.
- Решение (2026-05-19): профиль клиента включает `имя`, `телефон`, `Telegram username` (если есть), `Telegram ID` автоматически, `согласие на обработку ПД`, `комментарий/заметку клиента`.
- Решение (2026-05-19): для сценария записи приоритетен компактный формат `день + доступные времена` с визуально понятным выбором и возможностью перелистывания, а не длинный список и не отдельный тяжелый календарный экран.
- Решение (2026-05-19): в первой версии mini app должен поддерживать both `mobile Telegram` и `desktop Telegram`.
- Решение (2026-05-19): тренерский mini app в первой версии не ограничивается только заявками — в scope сразу входят `управление слотами`, `настройки записи`, `черный список`, `поиск клиентов`, `выгрузка`, чтобы mini app действительно был удобнее текущего bot UX.
- Решение (2026-05-19): реализация mini app будет идти поэтапно внутри одной общей цели, чтобы сохранить полноту функционала, но не повышать риск регресса для production.
- Итог (2026-05-19): концепция mini app согласована; дальнейшее проектирование можно переводить из продуктового уровня в UX-спецификацию и затем в техническое проектирование.

---

## Этап 20. Проектирование UX и структуры экранов mini app

Статус: `Завершен`

### Чек-лист текущего прогресса по этапу

- [x] Зафиксировать карту клиентских экранов mini app.
- [x] Зафиксировать карту тренерских экранов mini app.
- [x] Описать навигацию, пустые состояния, ошибки и статусы.
- [x] Описать адаптацию для mobile Telegram и desktop Telegram.
- [x] Согласовать, какие действия открываются как карточки, а какие — через верхнее меню mini app.

### Логи по этапу

- План (2026-05-19): на этом этапе нужно превратить уже согласованные требования в точную экранную карту: `главный экран`, `запись`, `мои записи`, `профиль`, `связь/помощь`, а также тренерские экраны по заявкам, слотам и настройкам.
- План (2026-05-19): отдельно нужно описать все состояния `ожидает подтверждения`, `подтверждено`, `отменено`, `предложен перенос`, чтобы проверить, как они помещаются на экраны и не перегружают интерфейс.
- Выполнено (2026-05-19): согласована детальная клиентская карта экранов mini app:
  - `технический вход` с определением роли и проверкой Telegram context;
  - `главный экран клиента` с приветственным блоком, логотипом, фото тренера, карточками `Записаться` и `Мои записи`;
  - `запись на тренировку` в формате `день + доступные времена` на одном экране;
  - `заявка отправлена / ожидание подтверждения`;
  - `мои записи` с карточками будущих заявок и тренировок;
  - `детали записи`;
  - `перенос записи`;
  - `запрос без слота`;
  - `профиль`;
  - `связь / помощь`;
  - результатные состояния `запись отменена`, `перенос принят`, `запрос на перенос отправлен`, `запрос без слота отправлен`.
- Выполнено (2026-05-19): согласована детальная тренерская карта экранов mini app:
  - `главный экран тренера`;
  - `заявки`;
  - `детали заявки`;
  - `предложить перенос`;
  - `будущие тренировки`;
  - `детали тренировки`;
  - `управление слотами`;
  - `открытие слотов`;
  - `закрытие слотов`;
  - `настройки записи`;
  - `черный список`;
  - `поиск клиентов / карточка клиента`;
  - `выгрузка`.
- Решение (2026-05-19): для клиента `Профиль` и `Связь/помощь` не выносятся в отдельные крупные карточки главного экрана, а открываются через верхнее меню mini app.
- Решение (2026-05-19): сценарий записи и сценарий переноса строятся без отдельного экрана подтверждения действия; выбранный слот визуально подсвечивается, а действие отправляется сразу с рабочего экрана.
- Решение (2026-05-19): `Мои записи` показывают только будущие записи и заявки, а статусы `ожидает подтверждения`, `подтверждено`, `отменено`, `предложен перенос` отображаются прямо в карточках.
- Решение (2026-05-19): если свободных слотов нет, клиенту сразу предлагается альтернативный путь `запрос без слота`, без тупикового пустого состояния.
- Решение (2026-05-19): если профиль клиента неполный, mini app не блокирует вход полностью, а мягко подводит к заполнению профиля перед записью.
- Решение (2026-05-19): для тренера mini app должен иметь быстрый доступ к `Заявкам`, `Тренировкам` и `Слотам`, а дополнительные административные функции открываются из главного тренерского экрана без лишней вложенности.
- Решение (2026-05-19): в мобильном Telegram интерфейс проектируется как одно-колоночный, а в desktop Telegram допускается более свободная раскладка карточек и списков, но без усложнения навигации.
- Итог (2026-05-19): UX-карта mini app согласована и может использоваться как основа для технического проектирования и последующей реализации.

---

## Этап 21. Техническое проектирование интеграции mini app

Статус: `Завершен`

### Чек-лист текущего прогресса по этапу

- [x] Зафиксировать способ авторизации через Telegram Mini App context без отдельного логина, кода и телефона.
- [x] Определить, какие существующие backend-ручки переиспользуются без изменений.
- [x] Определить минимально необходимые доработки API под mini app без ломки текущего бота.
- [x] Зафиксировать безопасную схему разработки и проверки mini app только через ветку `dev`.
- [x] Зафиксировать схему fallback: при проблемах mini app основной резервный путь остается через бот.

### Логи по этапу

- План (2026-05-19): mini app должен максимально переиспользовать текущий backend и предметную логику проекта; новые API-доработки допустимы только там, где они реально нужны для понятного и быстрого UX.
- План (2026-05-19): техническое проектирование mini app не должно менять production-схему `main -> autodeploy`, а вся дальнейшая разработка mini app идет в `dev`.
- Проектное решение (2026-05-19): mini app реализуется как отдельный frontend-клиент в `apps/mini-app`, который работает только внутри Telegram и использует уже существующий `apps/api` как основной backend без дублирования бизнес-логики в самом frontend.
- Проектное решение (2026-05-19): текущие внутренние API-ручки, которыми пользуется бот, можно переиспользовать по бизнес-смыслу, но не в исходной доверительной форме. Сейчас многие операции идентифицируют пользователя через `telegramId` / `trainerTelegramId` из `query` и `body`; для mini app такая схема недостаточно безопасна, потому что web-клиент нельзя считать доверенным так же, как внутренний bot layer.
- Проектное решение (2026-05-19): авторизация mini app строится на `Telegram Mini App initData`.
  - frontend получает `initData` из Telegram Web App context;
  - backend валидирует подпись `initData`;
  - после валидации backend определяет `telegramId`, `username`, базовую роль и не принимает эти значения как источник истины из произвольного тела запроса;
  - после успешной проверки backend выдает короткоживущую серверную mini-app-сессию или signed token для последующих API-вызовов mini app;
  - все дальнейшие mini-app-запросы должны выполняться уже в контексте этой серверной сессии, а не через ручную передачу `telegramId` в каждом действии.
- Проектное решение (2026-05-19): в первой версии mini app не требуется отдельная регистрация с логином, кодом или паролем; единственная точка доверия — подтвержденный Telegram context.
- Проектное решение (2026-05-19): bot и mini app используют одну и ту же БД и одну и ту же предметную логику, но mini app получает свой API-слой доступа поверх существующих сервисов NestJS:
  - bot может продолжать использовать текущие внутренние ручки с `telegramId/trainerTelegramId`;
  - mini app должен использовать либо новые `/mini-app/*` ручки, либо адаптированные публичные ручки с auth-guard и извлечением пользователя из mini-app-сессии;
  - прямое смешивание доверительных bot-endpoint-ов и публичного mini-app-трафика без слоя авторизации не допускается.
- Проектное решение (2026-05-19): роли в mini app определяются сервером.
  - обычный пользователь получает клиентский контур;
  - пользователь с `telegramId`, совпадающим с `TRAINER_TELEGRAM_ID` / `ADMIN_TELEGRAM_ID`, получает тренерский контур;
  - переключение `тренер -> клиентский режим` остается UI-возможностью, но не меняет серверную роль и права доступа.
- Анализ текущего API (2026-05-19): уже готовые сценарии, которые можно переиспользовать почти без изменений бизнес-логики:
  - `clients.findByTelegramId` / `clients.register` как база для первичного профиля;
  - `slots.available`, `slots.closure-info` для клиентской записи;
  - `bookings.request`, `bookings.client.trainings`, `bookings.client.cancel-training`, `bookings.client.reschedule-training`, `bookings.proposal.accept`, `bookings.proposal.decline` для клиентского контура;
  - `bookings.pending`, `bookings.confirm`, `bookings.reject`, `bookings.propose-time`, `bookings.cancel-training`, `bookings.reschedule-training`, `bookings.force-close`, `bookings.resync-calendar` для тренерского контура;
  - `slots.open`, `slots.close`, `slots.reopen`, `slots.trainer-grid`, `slots.closed-periods` для управления слотами;
  - `trainer-settings.current`, `trainer-settings.update` для базовых настроек записи;
  - `clients.blacklist`, `clients.blacklist.remove`, `clients.search` для списка black list и поиска клиентов;
  - `no-slot-requests.request` для клиентского сценария `запрос без слота`.
- Анализ текущего API (2026-05-19): текущего API недостаточно для полного mini app без доработок. Минимально необходимые новые или переработанные ручки:
  - mini-app auth endpoint для валидации `initData` и выдачи серверной сессии;
  - endpoint `me` / `profile` для чтения текущего профиля из mini-app-сессии без ручной передачи `telegramId`;
  - endpoint обновления профиля клиента, потому что сейчас есть только `register`, а отдельного редактирования профиля нет;
  - поддержка пользовательской `заметки клиента` в модели и API профиля, потому что это согласовано в UX, а в текущей Prisma-модели клиента такого поля нет;
  - trainer endpoint списка будущих тренировок, потому что сейчас есть клиентский список тренировок и список pending-заявок, но нет отдельной полноценной ручки под экран `Будущие тренировки` тренера;
  - trainer endpoint-ы для работы с `no-slot-requests`, потому что сейчас есть только клиентское создание запроса без слота, но нет списка/просмотра/архивации для тренера;
  - endpoint добавления клиента в черный список, потому что сейчас в API есть список и удаление, но не видно отдельной публичной ручки добавления;
  - endpoint выгрузки записей/тренировок, потому что согласованный экран `Выгрузка` уже входит в scope mini app, а отдельного export-модуля в текущем API нет;
  - при необходимости endpoint агрегированного главного экрана тренера, если на практике окажется удобнее получить суммарные counters/summary одним запросом, а не собирать экран из нескольких параллельных вызовов.
- Проектное решение (2026-05-19): схему данных допустимо минимально расширять только там, где это действительно требуется под согласованный UX mini app.
  - допустимое расширение: поле заметки клиента в `Client` или отдельная связанная сущность для клиентской заметки;
  - недопустимый путь: дублировать существующие booking/slot/training сущности только ради mini app.
- Проектное решение (2026-05-19): клиентский профиль mini app заполняется по схеме:
  - `telegramId` и доступный `username` подтягиваются из Telegram context;
  - `fullName`, `phone`, `consentAcceptedAt` берутся из текущего профиля, если клиент уже зарегистрирован;
  - если профиль отсутствует или неполный, mini app предлагает дозаполнить недостающее;
  - после этого mini app использует единый профиль клиента, а не отдельную web-версию профиля.
- Проектное решение (2026-05-19): уведомления первой версии остаются гибридными.
  - bot продолжает отправлять часть системных уведомлений;
  - mini app не требует отдельной push-инфраструктуры на первом релизе;
  - внутри mini app пользователь всегда видит актуальный статус через API.
- Проектное решение (2026-05-19): frontend mini app не должен знать и передавать `trainerTelegramId` как бизнес-идентификатор в открытом виде для контроля прав. Для тренерских экранов backend сам определяет, что сессия принадлежит тренеру, и только после этого разрешает административные операции.
- Проектное решение (2026-05-19): техническая реализация mini app идет в безопасной последовательности:
  - сначала auth/session слой mini app;
  - затем profile/me слой;
  - затем адаптация клиентских сценариев на mini-app-сессию;
  - затем адаптация тренерских сценариев;
  - затем только недостающие новые модули (`no-slot requests for trainer`, `export`, `blacklist add`, `trainer future trainings`).
- Ограничение первой версии (2026-05-19): отдельная внешняя web-доступность mini app вне Telegram не проектируется; поддержка браузерного standalone-режима вне Telegram не входит в текущую архитектуру этапа.
- Ограничение первой версии (2026-05-19): mini app не заменяет bot delivery для критичных уведомлений на первом релизе; это снижает риск и не требует отдельного notification backend.
- Итоговая техническая рамка (2026-05-19): mini app разрабатывается как безопасный публичный web-клиент над существующим NestJS backend, с обязательной серверной валидацией Telegram context, с максимальным переиспользованием текущих сервисов и только с точечными API/схемными доработками там, где они реально нужны для согласованного UX.
- Выполнено (2026-05-19): технические решения этапа переведены из проектного описания в кодовую базу `dev` без изменения production-ветки `main`.

---

## Этап 22. Реализация клиентского контура mini app

Статус: `Завершен`

### Чек-лист текущего прогресса по этапу

- [x] Реализовать главный клиентский экран mini app.
- [x] Реализовать профиль клиента с автоподтяжкой Telegram-данных и ручным редактированием.
- [x] Реализовать сценарий записи на тренировку в mini app.
- [x] Реализовать сценарий `мои записи` с просмотром только будущих тренировок.
- [x] Реализовать перенос, отмену и запрос без слота.
- [x] Реализовать пользовательские статусы и результатные состояния, включая `добавить в календарь`.

### Логи по этапу

- План (2026-05-19): клиентский mini app должен повторять функциональность бота по смыслу, но давать более быстрый, визуально понятный и компактный интерфейс без перегруженных текстовых сценариев.
- План (2026-05-19): в результате этого этапа клиент должен иметь возможность пройти основной путь записи полностью внутри mini app при штатной работе системы.
- Выполнено (2026-05-19): в `apps/api` добавлен mini-app auth/session слой с серверной валидацией Telegram `initData`, ручкой `me/profile` и dev-only входом для локальной разработки.
- Выполнено (2026-05-19): клиентские mini-app endpoint-ы переведены на безопасную mini-app-сессию без доверия к `telegramId` из публичного web-клиента.
- Выполнено (2026-05-19): в `apps/mini-app` реализованы клиентские экраны `главная`, `профиль`, `запись`, `мои записи`, `связь/помощь`.
- Выполнено (2026-05-19): реализованы клиентские сценарии `создание заявки`, `отмена`, `запрос на перенос`, `принятие/отклонение предложенного переноса`, `запрос без слота`, `добавить в календарь`.
- Выполнено (2026-05-19): подключены реальные брендовые ассеты mini app (`логотип`, `фото тренера`) и стили в палитре сайта школы.
- Self-check (2026-05-19): успешно выполнены `corepack pnpm --filter @tvoy-box/api prisma:generate`, `corepack pnpm --filter @tvoy-box/api typecheck`, `corepack pnpm --filter @tvoy-box/api build`, `corepack pnpm --filter @tvoy-box/mini-app typecheck`, `corepack pnpm --filter @tvoy-box/mini-app build`.
- Доработка client UI (2026-05-20 - 2026-05-21): все согласования по внешнему виду клиентского mini app относятся именно к этому этапу, а не к `Этапу 24`.
  - переработаны шрифты, мобильная плотность и структура главного экрана;
  - очищены экран `Профиль`, экран `Запись` и экран `Мои записи`;
  - вынесено согласие на обработку данных в запись;
  - добавлены блок связи с тренером, помощь и прямой Telegram-контакт;
  - добавлен `.ics`-файл с напоминаниями и возможность скрывать записи из списка клиента;
  - hero клиента переведен в более философский и брендовый стиль школы.

---

## Этап 23. Реализация тренерского контура mini app

Статус: `Завершен`

### Чек-лист текущего прогресса по этапу

- [x] Реализовать экран списка заявок тренера.
- [x] Реализовать подтверждение, отклонение и предложение переноса по заявке.
- [x] Реализовать просмотр будущих тренировок.
- [x] Реализовать управление слотами.
- [x] Реализовать настройки записи.
- [x] Реализовать черный список, поиск клиентов и выгрузку.

### Логи по этапу

- План (2026-05-19): mini app для тренера делается не как урезанная витрина, а как более удобный интерфейс над уже существующими админскими сценариями бота.
- План (2026-05-19): цель этапа — перенести в mini app весь ключевой тренерский контур, который в боте уже существует, но воспринимается менее удобно с точки зрения ежедневной работы.
- Выполнено (2026-05-19): в `apps/api` добавлен guard по роли mini app и защищённые тренерские mini-app маршруты для заявок, тренировок, слотов, настроек, клиентов, чёрного списка, запросов без слота и CSV-выгрузки.
- Выполнено (2026-05-19): в backend добавлены недостающие сервисные возможности под mini app:
  - список будущих тренировок тренера;
  - список и обновление `no-slot requests` для тренера;
  - добавление клиента в чёрный список;
  - CSV-выгрузка данных тренера;
  - выравнивание прав `trainer/admin` в сервисах слотов, настроек и клиентов.
- Выполнено (2026-05-19): в `apps/mini-app` реализован отдельный тренерский интерфейс с экранами `главная`, `заявки`, `будущие тренировки`, `слоты`, `клиенты`, `настройки`, `запросы без слота`, `профиль доступа`, `подсказки`.
- Выполнено (2026-05-19): на тренерском UI реализованы действия `подтвердить`, `отклонить`, `предложить другое время`, `отменить тренировку`, `предложить перенос`, `жёстко закрыть`, `ресинк календаря`, `открыть/закрыть слоты`, `диапазонное управление`, `поиск клиентов`, `добавление/снятие чёрного списка`, `обновление no-slot requests`, `CSV-выгрузка`.
- Self-check (2026-05-19): повторно успешно выполнены `corepack pnpm --filter @tvoy-box/api typecheck`, `corepack pnpm --filter @tvoy-box/api build`, `corepack pnpm --filter @tvoy-box/mini-app typecheck`, `corepack pnpm --filter @tvoy-box/mini-app build`.
- Доработка trainer UI (2026-05-20 - 2026-05-21): все согласования по внешнему виду и логике тренерского mini app относятся именно к этому этапу, а не к `Этапу 24`.
  - главное меню упрощено до `Заявки`, `Тренировки`, `Настройки`;
  - `Запросы без слота` перенесены в `Заявки`;
  - блок `Тренировки` перестроен под рабочий сценарий тренера;
  - блок `Настройки` перестроен вокруг `Параметров записи` и `Режима работы`;
  - экран `Слоты` приведен к логике `Открыт / Закрыт` на русском;
  - переработаны первый экран тренера, карточки обзора и мобильная версия;
  - добавлены прямые ссылки для связи с клиентом через Telegram.
- Доработка trainer logic + visual (2026-05-21): найден и исправлен системный сценарий переоткрытия отмененных слотов.
  - в trainer UI переключатель одиночного слота больше не использует отдельный `reopen`-маршрут только для ручных закрытий;
  - теперь повторное открытие отмененного/закрытого слота идет через общий `openSlots`, чтобы тренер мог вернуть в работу отмененное время без скрытого различия между `manual close` и `cancelled booking`;
  - на главных экранах trainer/client дополнительно уменьшен фирменный hero-заголовок, а правый тренерский блок смягчен по размеру карточек и watermark, чтобы композиция стала ближе к референсу сайта школы и не спорила с шапкой/логотипом.
- Доработка trainer fallback по календарю (2026-05-22): в карточки `Заявок` добавлен отдельный ручной `.ics`-файл календаря.
  - тренер может скачать календарную запись напрямую из заявки, даже если живая синхронизация с Google Calendar временно не сработала;
  - fallback реализован и в локальном preview, и в backend mini-app маршрутах, чтобы потом не переделывать логику на живом контуре;
  - в `.ics` сохраняются напоминания за `1 день` и за `1 час`.
- Корректировка trainer/client UI (2026-05-22): после дополнительного согласования интерфейса внесены точечные правки визуала и логики.
  - у клиента выбранный слот на экране записи переведен в зелёный цвет, чтобы состояние выбора читалось привычнее;
  - у тренера `.ics`-fallback убран из `Заявок` и перенесен в `Тренировки`, где он действительно нужен как ручной резерв вместо несработавшей календарной синхронизации;
  - правый блок на главном экране тренера дополнительно уменьшен и смягчен: карточка `Ближайшее занятие` стала компактнее, а текст внутри обзорных карточек — менее контрастным.
- Backend-диагностика trainer flow (2026-05-21): подтвержден отдельный инфраструктурный дефект модели данных.
  - у `Training.slotId` был установлен `unique`, из-за чего после отмены тренировки новая заявка на тот же слот могла падать при подтверждении;
  - схема Prisma изменена: `Training.slotId` больше не уникален, вместо этого оставлен обычный индекс по `slotId`;
  - `prisma db push` успешно синхронизировал БД под новую схему.
- Backend-диагностика trainer slots (2026-05-21): найдены и учтены stale `BOOKED`-слоты без активной заявки/тренировки.
  - в `SlotsService.openSlots` добавлена проверка реальной активной занятости слота;
  - если слот формально помечен как `BOOKED`, но не связан с живой `PENDING/CONFIRMED/RESCHEDULED` заявкой или `SCHEDULED/RESCHEDULED` тренировкой, он больше не блокирует повторное открытие тренером.
- Live-check trainer API (2026-05-21): после правки схемы и логики backend подтверждена реальная проверка через HTTP.
  - `POST /mini-app/session/dev-login` отдает trainer session;
  - `POST /mini-app/trainer/bookings/confirm` успешно подтвердил pending-заявку `cmpfg5clk0003vr9k4ldsv29b` со статусом `201 confirmed`;
  - `POST /mini-app/trainer/slots/open` успешно переоткрыл отмененный слот `2026-05-25 09:00` (`created=0, reopened=1`).

---

## Этап 24. Интеграция, тестирование и подготовка mini app к dev-развертыванию

Статус: `В работе`

### Чек-лист текущего прогресса по этапу

- [ ] Завершить клиентские сценарии mini app end-to-end на актуальном UI.
- [ ] Завершить тренерские сценарии mini app end-to-end на актуальном UI.
- [ ] Проверить совместимость `mobile Telegram` и `desktop Telegram`.
- [x] Проверить, что mini app не ломает текущие сценарии бота и не нарушает production-схему.
- [x] Подготовить безопасное включение mini app с возможностью fallback в бот.
- [x] Стабилизировать локальный preview/runtime для согласования интерфейса без зависимости от падающего backend.
- [ ] Стабилизировать полный локальный runtime `PostgreSQL + API + mini app`, чтобы проверять уже не preview, а живую интеграцию без ручного подъема.
- [ ] Подготовить mini app к следующему этапу выноса в GitHub и dev-деплоя на VPS.

### Логи по этапу

- Решение по структуре журнала (2026-05-21): все доработки клиентского UI относятся к `Этапу 22`, а все доработки trainer UI — к `Этапу 23`. `Этап 24` оставлен только для интеграции, runtime, bot-fallback, dev/VPS-подготовки и сквозной проверки системы.
- План (2026-05-19): финальный этап mini app должен завершаться не только визуальной готовностью, но и подтверждением, что действующий бот продолжает корректно работать как резервный канал и как источник части уведомлений первой версии.
- План (2026-05-19): запуск mini app должен быть мягким и контролируемым, без нарушения текущей production-схемы и без регресса для уже работающих пользовательских сценариев.
- Выполнено (2026-05-19): локальная PostgreSQL поднята через `corepack pnpm dev:db:up`; `corepack pnpm db:runtime-check` завершился `Prisma runtime-check: OK`.
- Выполнено (2026-05-19): схема БД синхронизирована под mini app через `prisma db push`; новый профиль клиента с `note` и mini-app-расширения доступны в локальной БД.
- Выполнено (2026-05-19): на живом старте API обнаружена и исправлена runtime-проблема DI в `MiniAppModule` (`AppConfigService` для `MiniAppAuthService`); после правки API успешно стартует и отвечает по `GET /health`.
- Интеграционная проверка backend (2026-05-19): подтвержден клиентский mini-app flow через реальные HTTP-вызовы:
  - `session/dev-login` клиента;
  - `me/profile` с созданием профиля;
  - открытие слота тренером;
  - просмотр слота клиентом;
  - создание заявки клиентом;
  - просмотр pending-заявки тренером;
  - подтверждение заявки тренером;
  - появление подтвержденной записи у клиента в `Мои записи`.
- Интеграционная проверка backend (2026-05-19): подтвержден тренерский mini-app flow через реальные HTTP-вызовы:
  - чтение и обновление `trainer/settings`;
  - создание клиентом `no-slot request`;
  - просмотр и обновление `no-slot requests` тренером;
  - поиск клиентов;
  - добавление и удаление клиента из `blacklist`;
  - получение `CSV export` с `Content-Type: text/csv; charset=utf-8`.
- Self-check (2026-05-19): общий `corepack pnpm build` по workspace завершился успешно, включая `apps/api`, `apps/mini-app` и `apps/bot`; на уровне сборки регресс для текущего бота не обнаружен.
- Выполнено (2026-05-19): подготовлен production-ready mini-app service для Docker/VPS:
  - добавлен `infra/docker/Dockerfile.mini-app`;
  - mini app включен в `deploy/compose.server.yml`;
  - deploy-скрипты обновлены на сборку, запуск и health-check mini app;
  - `docs/server-deploy.md` обновлен под домен `app.anyatobolova.ru`, localhost-порт mini app и проверочные команды.
- Выполнено (2026-05-19): обнаружен и устранен критичный риск production-конфигурации:
  - переменная `APP_PORT` конфликтовала между API runtime и host-портом mini app;
  - server deploy переведен на отдельные `MINI_APP_BIND_IP` и `MINI_APP_PORT`, чтобы mini app не мог незаметно сломать запуск API на VPS.
- Выполнено (2026-05-22): локальный режим согласования интерфейса отвязан от нестабильного backend-runtime.
  - в `apps/mini-app` добавлен stateful preview-слой с сохранением состояния в `localStorage`;
  - на `localhost/127.0.0.1` клиентский и тренерский контуры теперь открываются даже при падении `API` или `PostgreSQL`;
  - preview покрывает ключевые сценарии согласования: заявки, тренировки, слоты, настройки, поиск клиента, запросы без слота, клиентские записи и `.ics`-файл календаря;
  - это решение считается временным инструментом согласования UX и не заменяет следующий обязательный шаг по полной живой интеграции `bot + API + DB + Telegram`.
- Проверка preview (2026-05-22): локально подтверждено через браузерное открытие и ручной прогон экранов.
  - `http://127.0.0.1:3001/?dev=trainer` открывает trainer preview со стабильными ссылками;
  - `http://127.0.0.1:3001/` и `http://127.0.0.1:3001/?dev=client` открывают client preview;
  - подтверждение заявки в trainer preview переносит запись из `Заявок` в клиентские `Мои записи`;
  - в client preview у подтвержденной записи доступна кнопка `Добавить в календарь`, у отмененной и подтвержденной записи доступно `Удалить из списка`;
  - в trainer preview подтверждены короткий формат `Ближайшее занятие`, дата-only фильтр слотов и работа карточек `Открыт/Закрыт` в пределах рабочего режима.
- Ограничение preview (2026-05-22): Google Calendar в локальном режиме согласования не считается подключенным.
  - кнопка общей пересинхронизации у тренера больше не показывает ложный `success`, если работа идет в локальном preview;
  - в preview теперь честно сообщается, что проверка календарной синхронизации переносится на живой backend/VPS-контур;
  - это отдельно зафиксировано как обязательная часть следующей сквозной проверки в `Этапе 28`.
- Дополнение по trainer calendar fallback (2026-05-22): на период до полной живой проверки Google Calendar у тренера в `Заявках` доступен резервный путь через ручное скачивание `.ics`.
- Доработка trainer visual (2026-05-22): правый блок на главном экране тренера дополнительно смягчен.
  - карточки summary уменьшены по размеру;
  - текст внутри карточек стал менее контрастным и менее шумным;
  - правый блок сдвинут ближе к спокойной композиции референсного сайта школы и больше не перетягивает внимание с основного философского заголовка.
- Self-check (2026-05-19): `docker compose --env-file .env.server.example -f deploy/compose.server.yml config` успешно валидирует серверный stack c mini app после разделения портовых переменных.
- Трудности (2026-05-19): локальный `mini-app` frontend штатно собирается и при прямом запуске пишет `Next.js Ready`, но в текущей Windows-сессии не удаётся стабильно удержать локальный web-хост на `localhost:3001` для браузерного smoke-test.
- Примечание к истории лога (2026-05-21): подробные записи ниже по client UI и trainer UI сохранены как историческая расшифровка старого журнала, но по смыслу их нужно читать как результат `Этапов 22-23`, а не как самостоятельную новую область работ `Этапа 24`.
- Доработка UI (2026-05-20): по результатам локального просмотра клиентского интерфейса выполнена визуальная чистка главного экрана mini app:
  - убрана навязчивая dev-форма входа из обычного сценария локального просмотра;
  - подключен новый фирменный знак `Твой Бокс` с более читаемой шапкой;
  - в шапке зафиксировано бренд-оформление `ТВОЙ` / `БОКС` и подпись `Твой путь к силе и уверенности`;
  - на главном экране оставлены только две основные карточки: `Запись` и `Мои тренировки`.
- Доработка UI (2026-05-20): клиентский профиль и запись приведены ближе к согласованному UX:
  - из профиля убран служебный `ID`;
  - согласие на обработку данных перенесено из профиля в экран записи;
  - выбор даты и времени на экране записи теперь блокируется, пока пользователь не подтвердит согласие;
  - согласие сохраняется в профиле клиента и повторно используется для следующих записей.
- Доработка UI (2026-05-20): экран записи дополнен динамическим текстом по реальным настройкам тренера:
  - показывается горизонт записи вперед;
  - показывается ограничение записи в день тренировки (`sameDayBookingCutoff`), если оно задано;
  - убраны лишние количественные бейджи по времени в карточках дней.
- Доработка UI (2026-05-20): проведена типографическая правка клиентского mini app:
  - заменены слишком тяжёлые шрифтовые сочетания на более читаемую пару для заголовков и основного текста;
  - смягчены веса заголовков, карточек, кнопок и форм;
  - убраны лишние служебные надписи, мешающие оценивать реальный интерфейс страниц.
- Доработка UI (2026-05-20): экран `Мои записи` очищен от лишней строки `До ... · заявка без тренировки`; добавлена явная кнопка `Назад` внутри экрана, чтобы возврат был доступен не только через верхнюю шапку.
- Доработка trainer UI (2026-05-20): главное меню тренера упрощено до трех карточек `Заявки`, `Тренировки`, `Настройки`; внутри `Заявок` оставлены только активные согласования и переносы, а подтвержденные записи переводятся в блок `Тренировки`.
- Доработка trainer UI (2026-05-20): блок `Тренировки` приведен к согласованной структуре:
  - верхний ресинк календаря вынесен в общий action-блок рядом с `Обновить`;
  - кнопки переименованы в `Отменить`, `Предложить перенос`, `Удалить`;
  - добавлена быстрая ссылка `Написать клиенту`;
  - в строке времени убран текстовый период `до ...`, оставлено компактное отображение даты и старта.
- Backend-правка trainer flow (2026-05-20): список `Тренировки` для тренера расширен на подтвержденные, отмененные и перенесенные записи; архивированные тренером записи исключаются из списка через `trainerArchivedAt`.
- Backend-правка trainer flow (2026-05-20): архивирование тренировки для тренера сделано идемпотентным.
  - повторное удаление одной и той же карточки больше не должно падать ошибкой `Booking is already archived for trainer`;
  - backend возвращает успешный статус `archived`, если запись уже была скрыта из списка тренера.
- Backend-правка calendar sync (2026-05-20): отмена события в Google Calendar теперь считает `HTTP 410 reason=deleted` безопасным состоянием, а не критической ошибкой. Это закрывает кейс, когда событие уже было удалено в календаре раньше.
- Доработка trainer UI (2026-05-20): блок `Настройки` визуально перестроен по согласованной структуре:
  - `Параметры записи` подняты вверх;
  - `Cutoff на запись день-в-день, часов` переименован в `Ограничение на запись день-в-день, часов`;
  - добавлен отдельный визуальный блок `Режим работы` с днями и рабочим временем как следующий согласованный подшаг.
- Ограничение/решение (2026-05-20): полноценное сохранение `Режима работы` и его влияние на генерацию слотов в backend пока не включены в код намеренно.
  - причина: локальный Prisma-слой проекта требует отдельной безопасной доработки схемы/клиента, и в текущем витке нельзя включать полуготовую реализацию, которая может создать расхождение между UI и реальным поведением слотов;
  - решение: интерфейс `Режима работы` добавлен для согласования структуры, а backend-подключение будет вынесено в следующий технический подэтап после подтверждения UX.
- Доработка trainer UI (2026-05-20): в `Слотах` оставлена только русская легенда `Открыт` / `Закрыт` с цветовыми маркерами; блок `Периоды ручного закрытия` убран как дублирующий диапазонное управление.
- Процесс/журнал (2026-05-20): возобновлено обязательное ведение файла этапов при каждой заметной итерации по mini app; текущие локальные правки по trainer UI и backend снова зафиксированы в журнале этапа.
- Диагностика runtime (2026-05-20): при очередной проверке trainer-контура подтверждено, что часть согласованных правок уже присутствовала в исходниках, но локально у пользователя продолжал открываться неактуальный экземпляр `mini app` / `api`, из-за чего экран и замечания расходились с текущим состоянием кода.
- Выполнено (2026-05-20): с повышенным доступом проверены и перезапущены локальные фоновые задачи `CodexMiniApi` и `CodexMiniApp`; после перезапуска снова подтверждены ответы:
  - `GET http://127.0.0.1:3000/health -> 200`;
  - `GET http://127.0.0.1:3001/?dev=trainer -> 200`;
  - `GET http://127.0.0.1:3001/ -> 200`.
- Выполнено (2026-05-20): из локальной dev-БД удалены тестовые записи клиента `Никита` (`telegramId=7654592360`), чтобы старые bot-данные больше не мешали проверке trainer-блока.
  - удалены связанные `calendar_sync_logs`, `trainings`, `bookings`, `no_slot_requests` и карточка клиента;
  - повторная SQL-проверка вернула `0 строк` по `fullName ILIKE '%Никита%'` и `telegramId = 7654592360`.
- Решение (2026-05-20): дальнейшие замечания по trainer UI теперь сверяются только после принудительного обновления локальных фоновых задач, чтобы не путать реальные недоработки интерфейса со старым локальным рантаймом.
- Self-check (2026-05-20): после UI-правок успешно выполнены:
  - `corepack pnpm --filter @tvoy-box/api prisma:generate`;
  - `corepack pnpm --filter @tvoy-box/api typecheck`;
  - `corepack pnpm --filter @tvoy-box/api build`;
  - `corepack pnpm --filter @tvoy-box/mini-app typecheck`;
  - `corepack pnpm --filter @tvoy-box/mini-app build`.
- Self-check (2026-05-20): после trainer-правок повторно успешно выполнены:
  - `corepack pnpm --filter @tvoy-box/api typecheck`;
  - `corepack pnpm --filter @tvoy-box/api build`;
  - `corepack pnpm --filter @tvoy-box/mini-app typecheck`;
  - `corepack pnpm --filter @tvoy-box/mini-app build`.
- Self-check (2026-05-20): локальный API повторно поднят и подтвержден по `GET http://127.0.0.1:3000/health -> 200`; локальный mini app продолжает отвечать на `http://127.0.0.1:3001`.
- Доработка client UI (2026-05-20): в клиентском контуре добавлен отдельный блок связи с тренером на главном экране и внутри `Помощи`.
  - под карточками `Запись` и `Мои тренировки` выведен отдельный блок с текстом для прямого контакта;
  - используется живой Telegram-контакт тренера из mini-app session (`supportContact`);
  - в экране `Помощь` сохранен возврат через `Назад`, а ниже списка добавлена отдельная CTA-ссылка `Написать тренеру`.
- Backend + client flow (2026-05-20): клиентский mini app дополнен прямой выгрузкой календарного файла `.ics` для тренировок.
  - добавлен backend endpoint `GET /mini-app/client/trainings/calendar`;
  - файл формируется на сервере по записи клиента и отдается на скачивание из mini app;
  - внутрь `.ics` сразу включены два напоминания: за `1 день` и за `1 час` до тренировки.
- Backend + client flow (2026-05-20): клиенту разрешено скрывать записи из собственного списка независимо от текущего статуса, чтобы `Мои записи` не копились бесконечно.
  - для этого расширен client archive-flow;
  - в интерфейсе `Мои записи` выведена кнопка `Удалить из списка` для всех записей, которые backend помечает как удаляемые.
- Доработка client UI (2026-05-20): главный экран клиента уплотнен по вертикали.
  - контент hero-блока поднят выше;
  - сетка hero переведена на выравнивание по верхнему краю, чтобы перед приветствием не оставалось лишнего пустого пространства.
- Self-check (2026-05-20): после клиентских правок и добавления calendar-download повторно успешно выполнены:
  - `corepack pnpm --filter @tvoy-box/api typecheck`;
  - `corepack pnpm --filter @tvoy-box/api build`;
  - `corepack pnpm --filter @tvoy-box/mini-app typecheck`;
  - `corepack pnpm --filter @tvoy-box/mini-app build`.
- Runtime-восстановление (2026-05-21): локальный просмотр mini app перестал открываться, потому что одновременно упали `api`, `mini app` и portable PostgreSQL.
- Диагностика (2026-05-21):
  - `http://127.0.0.1:3000/health` не отвечал;
  - `http://127.0.0.1:3001/` не открывался;
  - Docker daemon был недоступен, поэтому штатный `watchdog` не смог поднять БД через обычный сценарий.
- Выполнено (2026-05-21): portable PostgreSQL восстановлен вручную обходным запуском через `cmd /c start ... powershell ... postgres.exe -D ...`; после этого `pg_isready` подтвердил `localhost:5432 - принимает подключения`.
- Выполнено (2026-05-21): локальный API повторно поднят в фоне; подтвержден ответ `GET http://127.0.0.1:3000/health -> 200`.
- Выполнено (2026-05-21): подвисший dev-процесс mini app на порту `3001` принудительно завершен и запущен заново; после чистого рестарта получен ответ `GET http://127.0.0.1:3001/ -> 200`, а `next dev` подтвердил `Ready`.
- Доработка client UI (2026-05-21): первый экран клиента переведен на более философский и брендовый tone of voice.
  - убрана строка приветствия `Здравствуйте, ...`;
  - hero-заголовок заменен на `Сила начинается не с удара. Сила начинается с уверенности в себе.`;
  - подзаголовок заменен на спокойный брендовый текст без агрессивных спортивных формулировок;
  - блок `Связь с тренером` и CTA в `Помощи` переведены на прямую ссылку `https://t.me/RostPV`.
- Доработка client UI (2026-05-21): уменьшен и уплотнен contact CTA клиента.
  - кнопка `Написать тренеру` больше не должна выглядеть как широкая полоска на весь экран;
  - ссылка остается одинаковой и на главной, и внутри `Помощи`.
- Доработка client UI (2026-05-21): hero-блок клиента дополнительно поджат по вертикали, чтобы контент поднимался выше и на первом экране не было лишнего пустого воздуха.
- Доработка trainer UI (2026-05-21): у тренера включен согласованный новый главный экран без большой фотографии.
  - справа вместо фото используется блок `Мысль тренера / Сегодняшний настрой`;
  - выведены динамические карточки `Сегодня тренировок`, `Новых заявок`, `Ближайшее занятие`;
  - добавлен слабый watermark знака школы.
- Доработка trainer UI (2026-05-21): `Запросы без слота` окончательно перенесены в `Заявки`, а не в `Настройки`.
  - в карточке запроса теперь явно поясняется, что комментарий тренера сохраняется в mini app;
  - для реального ответа клиенту используется кнопка `Написать в Telegram`, то есть текущий поток остается ручным через личный диалог, без автоматической доставки комментария клиенту внутрь mini app или бота.
- Доработка trainer UI + backend (2026-05-21): согласованный блок `Режим работы` перестал быть только визуальной заготовкой.
  - в Prisma/`trainer-settings` добавлены `workingDays`, `workdayStartHour`, `workdayEndHour`;
  - `mini-app` trainer settings теперь реально читает и сохраняет рабочие дни и рабочее время;
  - генерация/показ слотов переведены на учет согласованных рабочих дней и рабочего диапазона часов, чтобы в сетке не появлялись ночь и лишние дни.
- Доработка trainer UI (2026-05-21): визуально усилено различие `Открыт / Закрыт` в экране `Слоты`.
  - `Открыт` получил более заметную зеленую подсветку;
  - `Закрыт` получил более заметную красную подсветку;
  - легенда `Открыт / Закрыт` оставлена на русском как основной ориентир для тренера.
- Техническая доработка runtime (2026-05-21): `scripts/dev/watchdog-loop.ps1` переведен на запуск сервисов через корневые `corepack pnpm dev:*` команды в отдельных hidden PowerShell-процессах, а не через промежуточный `cmd`-слой с менее надежным удержанием PID.
- Техническая доработка runtime (2026-05-21): `scripts/dev/postgres-runner.ps1` переведен с прямого `postgres.exe -D ...` на `pg_ctl start -w`, чтобы portable PostgreSQL поднимался через явное ожидание готовности.
- Диагностика watchdog (2026-05-21): несмотря на доработки, полный сценарий `corepack pnpm dev:watchdog:start` в текущем Windows-окружении всё еще нестабилен именно на portable PostgreSQL после полного stop/start.
  - это уже локализовано как отдельный инфраструктурный блокер Windows portable DB, а не как проблема mini app frontend;
  - чтобы не блокировать согласование интерфейсов, локальные `db + api + mini app` были повторно подняты вручную.
- Выполнено (2026-05-21): после ручного восстановления локального runtime снова подтверждены рабочие ссылки:
  - `GET http://127.0.0.1:3000/health -> 200`;
  - `GET http://127.0.0.1:3001/ -> 200`.
- Runtime-восстановление (2026-05-21, повторный инцидент): локальные ссылки снова одновременно перестали открываться (`api`, `client`, `trainer`), `watchdog-status` снова показал полностью остановленный стек (`postgres/api/bot/mini-app = false`).
- Выполнено (2026-05-21): локальный стек снова поднят вручную через отдельный background-start:
  - portable PostgreSQL поднят отдельно;
  - `mini app` снова подтвержден по `GET http://127.0.0.1:3001/ -> 200` и `GET http://127.0.0.1:3001/?dev=trainer -> 200`;
  - `api` стабилизирован прямым запуском `apps/api/dist/main.js`, после чего повторно подтвержден `GET http://127.0.0.1:3000/health -> 200`.
- Диагностика (2026-05-21): в текущем Windows-окружении самый стабильный локальный сценарий пока выглядит так:
  - portable PostgreSQL поднимать отдельно;
  - `mini app` можно держать на `next dev`;
  - `api` надёжнее удерживается прямым запуском собранного `dist/main.js`, чем через обычный `dev:api`.
- Выполнено (2026-05-21, стабилизация локальных ссылок): `apps/mini-app` переведен на дополнительный локальный `start`-режим (`next start --port 3001`), а `next.config.ts` дополнен `allowedDevOrigins` для `127.0.0.1` и `localhost`.
- Выполнено (2026-05-21, повторный runtime-restore): локальный стек снова поднят уже в более устойчивом виде:
  - PostgreSQL поднят отдельно;
  - API поднят прямым запуском `apps/api/dist/main.js`;
  - mini app поднят через `corepack pnpm --filter @tvoy-box/mini-app start`.
- Self-check (2026-05-21): после перевода mini app на `start`-режим повторно подтверждены:
  - `GET http://127.0.0.1:3000/health -> 200`;
  - `GET http://127.0.0.1:3001/ -> 200`;
  - `GET http://127.0.0.1:3001/?dev=trainer -> 200`.
- Self-check (2026-05-21): после клиентских, trainer и runtime-правок успешно выполнены:
  - `corepack pnpm --filter @tvoy-box/api typecheck`;
  - `corepack pnpm --filter @tvoy-box/api build`;
  - `corepack pnpm --filter @tvoy-box/mini-app typecheck`;
  - `corepack pnpm --filter @tvoy-box/mini-app build`.
- Доработка trainer UI + slots/runtime (2026-05-21):
  - сводка на главном экране тренера скорректирована: `Сегодня тренировок` теперь считает только тренировки на текущую московскую дату, а `Ближайшее занятие` показывает ближайшую фактическую дату и время, а не только часы;
  - trainer date/time inputs переведены на явную интерпретацию `datetime-local` как московского времени, чтобы экран `Слоты` не уезжал по часам и дням из-за локального timezone браузера;
  - `GET /mini-app/trainer/slots` дополнительно ограничен рабочими днями и рабочими часами тренера, поэтому вне согласованного режима работы слоты больше не должны появляться;
  - в trainer slot grid все неоткрытые состояния (`CLOSED/BOOKED/HELD/...`) визуально сведены к красному `Закрыт`, а `OPEN` усилен до явного зелёного состояния;
  - в поиске клиентов добавлена живая ссылка `Написать в Telegram`.
- Доработка брендового UI (2026-05-21):
  - hero-слоган на главных экранах клиента и тренера уменьшен по размеру, чтобы он оставался фирменным акцентом, но меньше перетягивал внимание на себя.
- Runtime-restore (2026-05-21, после свежей сборки):
  - после новых правок заново пересобраны `api` и `mini-app`;
  - локальные процессы `apps/api/dist/main.js` и `@tvoy-box/mini-app start` перезапущены;
  - повторно подтверждены рабочие ответы `GET http://127.0.0.1:3000/health -> 200`, `GET http://127.0.0.1:3001/ -> 200`, `GET http://127.0.0.1:3001/?dev=trainer -> 200`.
- Доработка trainer UI (2026-05-21, повторное согласование):
  - `Ближайшее занятие` сокращено до компактного формата `ДД.ММ; ЧЧ:ММ`;
  - дублирующий eyebrow `ТВОЙ БОКС` на главном экране тренера убран;
  - верхний фильтр периода в `Слотах` переведен на выбор только дат без времени, чтобы экран был чище и проще для ежедневного использования.
- Runtime-фикс (2026-05-21, стабильность локальных ссылок):
  - найден и устранен дополнительный блокер старта `api`: сервис мог падать еще до `GET /health`, если не удавалось создать директорию runtime-логов;
  - `apps/api/src/common/logging/runtime-logger.ts` переведен в безопасный console-only fallback при недоступной директории/файле логов;
  - после этого `api` и `mini-app` заново пересобраны и подняты, а локальные URL снова подтверждены как рабочие.
- Runtime-восстановление (2026-05-21, повторная проверка ссылок):
  - после очередного падения локальных ссылок повторно подтверждено, что `mini-app` уже поднимается корректно на `3001`, а `api` снова стартует на `3000` после runtime-фикса логгера;
  - локальные ответы повторно проверены через `GET http://127.0.0.1:3000/health -> 200` и `GET http://127.0.0.1:3001/?dev=trainer -> 200`.
- Runtime-решение по песочнице (2026-05-21):
  - подтверждено, что фоновые локальные процессы `api` и `mini-app`, запущенные внутри обычной песочницы Codex, могут завершаться сразу после команды, из-за чего пользователь видит `ERR_CONNECTION_REFUSED`;
  - для стабильной локальной проверки интерфейсов `api` и `mini-app` были подняты вне песочницы как отдельные фоновые процессы;
  - после этого оба URL повторно удержались в рабочем состоянии: `127.0.0.1:3000 -> 200`, `127.0.0.1:3001/?dev=trainer -> 200`, а порты `3000/3001` подтверждены как `LISTENING`.
- Решение по расхождению экрана и кода (2026-05-21):
  - замечания пользователя вида `ничего не изменилось` в этом заходе были связаны не с отсутствием правок в trainer UI, а с тем, что локально открывался неактуальный или упавший экземпляр mini app;
  - после свежей пересборки и перезапуска нужно сверять trainer UI уже по новому живому экземпляру.
- Новый runtime-блокер Windows portable PostgreSQL (2026-05-21):
  - при попытке перевести локальный стек на постоянный `watchdog`-режим снова воспроизвелся системный сбой portable PostgreSQL в текущем Windows-окружении;
  - `dev:db:up` недоступен из-за выключенного Docker daemon;
  - `dev:watchdog:start` и `postgres-runner.ps1` подтверждают, что основной локальный риск сейчас не в mini app, а в нестабильном фоновом старте portable DB;
  - это остается открытым подэтапом `Этапа 24`, потому что напрямую влияет на стабильность локальных ссылок `127.0.0.1`.
- Следующий шаг soft launch (2026-05-19):
  - заполнить на VPS `.env.server` с `PUBLIC_APP_DOMAIN`, `MINI_APP_BIND_IP`, `MINI_APP_PORT`, `NEXT_PUBLIC_API_BASE_URL`, `MINI_APP_ALLOWED_ORIGINS`;
  - добавить или проверить proxy `app.anyatobolova.ru -> 127.0.0.1:3302` в системном `Caddy`;
  - выполнить deploy и проверить `https://app.anyatobolova.ru/`, затем открыть mini app из Telegram.
- Вывод (2026-05-19): backend-интеграция и серверная подготовка mini app подтверждены; открытыми остаются только визуальный smoke-test UI и живая проверка mini app внутри Telegram после выкладки на dev/VPS.

---

## Этап 25. Подготовка mini app к выносу в GitHub

Статус: `Завершен`

### Чек-лист текущего прогресса по этапу

- [x] Сверить ветку `dev` и структуру изменений по `api`, `bot`, `mini-app`, `deploy`, `docs`.
- [x] Проверить, что все env-переменные для mini app отражены в `.env.example` и `.env.server.example`.
- [x] Обновить документацию по локальному старту mini app, клиентскому и тренерскому режиму.
- [x] Подготовить чистый список того, что именно переносится в GitHub как следующий логический пакет.
- [x] Отделить временные локальные решения от того, что действительно должно попасть в постоянную схему проекта.

### Логи по этапу

- Цель этапа: до VPS и автодеплоя привести ветку `dev` в понятное и переносимое состояние, чтобы GitHub содержал осмысленный пакет mini app, а не смесь локальных экспериментов и runtime-обходов.
- Старт этапа (2026-05-22): после завершения локального согласования клиентского и тренерского интерфейсов начат переход от UI-итераций к подготовке чистого GitHub-пакета.
- Проверка пакета (2026-05-22): структура изменений в `dev` сверена по основным зонам проекта: `apps/api`, `apps/bot`, `apps/mini-app`, `deploy`, `docs`, `scripts`.
- Проверка env (2026-05-22): mini-app переменные подтверждены в `.env.example` и `.env.server.example`, включая `NEXT_PUBLIC_API_BASE_URL`, `MINI_APP_AUTH_SECRET`, `MINI_APP_ALLOWED_ORIGINS`, `MINI_APP_BIND_IP`, `MINI_APP_PORT` и календарные настройки.
- Подготовка к коммиту (2026-05-22): из пакета исключены временные локальные артефакты согласования интерфейса (`.playwright-mcp`, `runtime-logs`, локальные скриншоты и markdown-снимки браузера), чтобы в GitHub ушёл именно код и документация, а не технический мусор локальной проверки.
- Preview-стабилизация (2026-05-22): после очередного инцидента `ERR_CONNECTION_REFUSED` подтверждено, что для текущего этапа согласования интерфейса рабочим контуром остается локальный `mini-app preview` на `127.0.0.1:3001`, а не полный живой стек `db + api + bot`.
- Выполнено (2026-05-22): `apps/mini-app` повторно пересобран (`typecheck`, `build`) и перезапущен в фоне на `3001` уже после последних UI-правок, чтобы пользователь видел актуальный тренерский и клиентский экран, а не старый билд.
- Доработка UX (2026-05-22): в текущем GitHub-пакете дополнительно подтверждены и приведены к актуальному виду:
  - зеленый выбор слота у клиента;
  - резервная кнопка `.ics` у тренера в `Тренировках`, а не в `Заявках`;
  - более спокойные правые summary-карточки на главном экране тренера;
  - исправление битой кодировки в части trainer-preview сообщений.
- Документация (2026-05-22): `README.md` обновлен под реальное состояние проекта:
  - добавлены клиентский и тренерский `preview`-режимы;
  - разведены `preview` и живой локальный контур;
  - зафиксировано, что внешний Telegram-путь проверяется уже на VPS, а не локально.
- GitHub-пакет (2026-05-22): как следующий переносимый логический пакет зафиксированы:
  - код `apps/api`, `apps/bot`, `apps/mini-app`;
  - серверные артефакты `deploy`, `infra`, `scripts/deploy`;
  - документация `README.md`, `docs/server-deploy.md`, `Этапы разработки.md`;
  - без локальных preview-артефактов, временных скриншотов и фоновых runtime-логов.
- Разделение временного и постоянного (2026-05-22): зафиксировано, что локальный `preview` — это временный инструмент согласования интерфейса, а постоянной схемой проекта считается только внешний dev/VPS-контур вместе с Telegram mini app, backend и ботом.

---

## Этап 26. Dev-деплой mini app на VPS

Статус: `Завершен`

### Чек-лист текущего прогресса по этапу

- [x] Зафиксировать безопасную схему внешнего dev-контура: отдельный stack, отдельные порты и отдельные dev-поддомены.
- [x] Подготовить серверный `.env.server` для mini app.
- [x] Определить безопасную точку входа mini app в Telegram для dev-проверки.
- [x] Поднять mini app на `app.anyatobolova.ru` без влияния на production-бота.
- [x] Проверить reverse proxy, Docker-сервис и health-check mini app.
- [x] Проверить открытие mini app по HTTPS вне локального окружения.
- [x] Зафиксировать dev-адреса, команды и порядок ручной проверки.

### Логи по этапу

- Цель этапа: получить первый стабильный внешний dev-контур mini app, чтобы перестать зависеть от падающих локальных ссылок при согласовании интерфейса и сценариев.
- Старт этапа (2026-05-22): после закрытия GitHub-подготовки начат переход к внешнему dev-контуру mini app на VPS.
- Документация этапа (2026-05-22): `docs/server-deploy.md` дополнен отдельным разделом про dev-контур mini app перед Telegram-проверкой, чтобы не смешивать локальный `preview`, внешний `VPS` и production-ветку `main`.
- Диагностика (2026-05-22): текущий локальный `.env.server` оказался наследием старой production-схемы и не содержит полный набор mini-app переменных (`PUBLIC_APP_DOMAIN`, `NEXT_PUBLIC_API_BASE_URL`, `MINI_APP_ALLOWED_ORIGINS`, `MINI_APP_AUTH_SECRET`, webhook-поля и пр.), поэтому его нельзя считать готовой базой для safe dev-деплоя.
- Выполнено (2026-05-22): в репозиторий добавлен безопасный шаблон `deploy/.env.server.dev.example` для внешнего dev-контура mini app с отдельным `STACK_NAME`, отдельными портами и отдельными dev-доменами.
- Решение (2026-05-22): безопасный dev-контур mini app не должен использовать production-бота как точку входа по умолчанию. Для Telegram-проверки рекомендуется отдельный test bot, иначе dev-проверка начнет затрагивать production-сценарий.
- Диагностика (2026-05-22): сетевой доступ до VPS подтвержден (`22` порт доступен), но из текущей сессии пока не подтвержден рабочий SSH-вход под `deploy`: `codex`-ключ не авторизован на сервере, а `github-actions` ключ локально защищен ACL и не может быть использован без отдельного доступа к самому файлу.
- Попытка входа по паролю (2026-05-22): переход на парольный путь подтвержден как правильное направление для этого этапа, но самого значения пароля нет в доступных локальных артефактах текущей сессии. Следующий шаг возможен сразу после повторной передачи пароля пользователем или указания, где он сохранен локально.
- Выполнено (2026-05-22): подтвержден рабочий парольный вход на VPS под `root`, production-стек при этом не затронут.
- Выполнено (2026-05-22): на VPS развернут отдельный dev-стек mini app в каталоге `/opt/stack/tvoy-box-miniapp-dev` со своим `STACK_NAME=tvoy-box-miniapp-dev`, своей БД и отдельными локальными портами:
  - `api` -> `127.0.0.1:3310`
  - `mini-app` -> `127.0.0.1:3312`
- Выполнено (2026-05-22): `Caddy` на сервере перенастроен так, чтобы публичный адрес `https://app.anyatobolova.ru` больше не отдавал заглушку, а работал как dev-контур mini app:
  - `/mini-api/*` -> `127.0.0.1:3310`
  - `/` -> `127.0.0.1:3312`
- Проверка (2026-05-22): после первой серверной сборки найден и исправлен реальный инфраструктурный блокер в `infra/docker/Dockerfile.mini-app` — runtime-команда запускала `next start` из корня `/workspace`, из-за чего контейнер не видел production-build `.next`. После исправления контейнер `mini-app` стабильно перешел в состояние `healthy`.
- Проверка (2026-05-22): dev-контур mini app на VPS подтвержден рабочим по трем независимым точкам:
  - `curl http://127.0.0.1:3310/health` -> `200`
  - `http://127.0.0.1:3312/` -> `200`
  - `https://app.anyatobolova.ru/` -> `200`
- Зафиксированные dev-адреса (2026-05-22):
  - внешний mini app: `https://app.anyatobolova.ru/`
  - внешний trainer preview: `https://app.anyatobolova.ru/?dev=trainer`
  - внешний client preview: `https://app.anyatobolova.ru/?dev=client`
  - внутренний health API на VPS: `http://127.0.0.1:3310/health`
- Ограничение этапа (2026-05-22): в этом безопасном dev-контуре пока не запущен отдельный dev-бот и не настроен Telegram Mini App вход через реальный `initData`. На текущем шаге внешний URL уже готов для просмотра и согласования в браузере/Telegram browser, но полная точка входа `бот -> mini app` еще остается отдельной задачей этого этапа.
- Следующий практический шаг по этапу (2026-05-22): определить безопасную Telegram-точку входа для dev-проверки — либо отдельный test bot, либо заранее согласованный временный сценарий открытия `https://app.anyatobolova.ru/` внутри Telegram без затрагивания production-бота.
- Выполнено (2026-05-22, dev-login для внешнего контура): backend mini app расширен флагом `MINI_APP_ENABLE_DEV_LOGIN`, который разрешает технический `dev-login` только на выделенном VPS dev-стенде и не ослабляет production-контур.
- Выполнено (2026-05-22, внешний preview): frontend mini app больше не ограничивает `?dev=client` / `?dev=trainer` только `localhost`; на dev-VPS эти ссылки теперь напрямую создают соответствующую тестовую сессию через `POST /mini-api/mini-app/session/dev-login`.
- Выполнено (2026-05-22, проверка ролей): подтверждено, что trainer preview на `https://app.anyatobolova.ru/?dev=trainer` открывает именно тренерский интерфейс, а client preview на `https://app.anyatobolova.ru/?dev=client` — клиентский интерфейс.
- Выполнено (2026-05-22, статические ассеты): `apps/mini-app/next.config.ts` переведен на `images.unoptimized = true`, чтобы логотип и изображения mini app стабильно открывались за reverse proxy на внешнем домене без привязки к `/_next/image`.
- Инцидент dev-контура (2026-05-22): на внешнем `app.anyatobolova.ru` интерфейс открывался без логотипа и без фото тренера. Диагностика показала, что в текущем VPS release папка `apps/mini-app/public/assets` оказалась пустой, поэтому `/assets/logo-mark.png` и `/assets/trainer.png` отдавали `404`.
- Выполнено (2026-05-22, фикс ассетов): PNG-файлы `logo-mark.png`, `logo.png`, `trainer.png` дозалиты в текущий release dev-стека на VPS, `mini-app` пересобран и перезапущен.
- Проверка (2026-05-22, ассеты): подтверждено, что внешние URL `https://app.anyatobolova.ru/assets/logo-mark.png` и `https://app.anyatobolova.ru/assets/trainer.png` теперь отдают `HTTP 200`.
- Выполнено (2026-05-23, safe Telegram entry): до выделения отдельного test bot выбран безопасный временный сценарий ручной проверки внутри Telegram без использования production-бота:
  - клиентская ссылка: `https://app.anyatobolova.ru/?dev=client`
  - тренерская ссылка: `https://app.anyatobolova.ru/?dev=trainer`
  - ссылки открываются из `Избранного` или из личного технического чата внутри Telegram browser и не публикуются рабочим пользователям.
- Решение по этапу (2026-05-23): `Этап 26` закрыт как инфраструктурный dev-подэтап. Внешний контур поднят, роли работают, ассеты и технический `dev-login` на VPS проверены. Дальнейшая работа переносится в `Этап 27` (автодеплой из GitHub) и `Этап 28` (сквозная живая проверка внутри Telegram).

---

## Этап 27. Автодеплой mini app из GitHub

Статус: `В работе`

### Чек-лист текущего прогресса по этапу

- [x] Определить, как mini app включается в существующий GitHub Actions pipeline.
- [x] Добавить или обновить workflow для `dev`-деплоя mini app.
- [x] Проверить безопасный порядок выкладки `api -> mini-app -> bot`, если он нужен.
- [ ] Убедиться, что автодеплой mini app не ломает существующий production-autodeploy бота.
- [ ] Описать rollback-сценарий для mini app.

### Логи по этапу

- Цель этапа: перевести mini app из ручных локальных и серверных запусков в повторяемый и контролируемый pipeline.
- Старт этапа (2026-05-23): после закрытия `Этапа 26` начата подготовка отдельного GitHub Actions pipeline для dev mini app без влияния на production-автодеплой ветки `main`.
- Выполнено (2026-05-23): в репозиторий добавлен отдельный workflow `.github/workflows/deploy-dev-miniapp.yml`.
  - триггер: `push` в ветку `dev` и `workflow_dispatch`;
  - deploy root: `/opt/stack/tvoy-box-miniapp-dev`;
  - bootstrap root: `/opt/stack/tvoy-box-miniapp-dev/current`;
  - проверки после выкладки: `https://app.anyatobolova.ru/mini-api/health` и `https://app.anyatobolova.ru/`.
- Выполнено (2026-05-23): `scripts/deploy/deploy-server.sh` расширен флагом `DEPLOY_WITH_BOT`.
  - production по умолчанию остается в старом режиме и поднимает `migrate + api + mini-app + bot`;
  - dev-workflow mini app передает `DEPLOY_WITH_BOT=false` и поднимает только `migrate + api + mini-app`.
- Решение по этапу (2026-05-23): безопасный порядок выкладки для dev mini app зафиксирован как `postgres -> build -> migrate -> api + mini-app`, без автозапуска бота. Это снижает риск случайно затронуть production Telegram-контур при каждом `push` в `dev`.
- Документация (2026-05-23): `docs/auto-deploy.md` обновлен и теперь явно разводит production-автодеплой (`main`) и dev-автодеплой mini app (`dev`).
- Техническое ограничение self-check (2026-05-23): локальная shell-проверка `bash -n scripts/deploy/deploy-server.sh` в текущем Windows-окружении не выполнена, потому что в сессии отсутствует локальный `/bin/bash`. Сам скрипт проверен структурно по внесенным изменениям и будет окончательно подтвержден первым прогоном GitHub Actions workflow.
- Инцидент первого прогона (2026-05-23): первый реальный запуск workflow `Deploy Dev Mini App` упал на шаге `Run remote deploy`.
  - Причина 1: каталог `/opt/stack/tvoy-box-miniapp-dev` на VPS был создан от `root`, а GitHub workflow заходит под `deploy`, поэтому у `deploy` не было прав на запись в `releases` и `shared`.
  - Причина 2: dev-контур использовал свой `.env.server`, но Google service account для календаря физически лежал только в production-каталоге бота, из-за чего bootstrap `shared/.secrets` для dev-контура был неполным.
- Исправление по этапу (2026-05-23): права на `/opt/stack/tvoy-box-miniapp-dev` приведены к `deploy:deploy`, а bootstrap в `scripts/deploy/remote-deploy.sh` разделен на три независимых источника:
  - `BOOTSTRAP_ENV_ROOT` — для dev `.env.server`;
  - `BOOTSTRAP_SECRETS_ROOT` — для `google-service-account.json`;
  - `BOOTSTRAP_LOGS_ROOT` — для логов.
- Исправление по workflow (2026-05-23): `.github/workflows/deploy-dev-miniapp.yml` теперь явно передает:
  - `BOOTSTRAP_ENV_ROOT=/opt/stack/tvoy-box-miniapp-dev/current`;
  - `BOOTSTRAP_SECRETS_ROOT=/opt/stack/tvoy-box-bot`;
  - `BOOTSTRAP_LOGS_ROOT=/opt/stack/tvoy-box-miniapp-dev/current`.
- Решение по этапу (2026-05-23): автодеплой `dev` должен использовать dev-окружение mini app как источник переменных окружения, но production-бот как источник единого Google service account. Это исключает случайное наследование production `.env.server` и одновременно не ломает календарную интеграцию.
- Проверка под пользователем deploy (2026-05-23): исправленный `remote-deploy.sh` воспроизведен вручную на VPS именно под `deploy`, без root-обхода. Релиз `manual-test-20260523` успешно развернут в `/opt/stack/tvoy-box-miniapp-dev/releases/manual-test-20260523`, `shared/.env.server` и `shared/.secrets/google-service-account.json` созданы автоматически.
- Наблюдение по GitHub Actions (2026-05-23): второй реальный прогон workflow на commit `bf15d92` дошел дальше `Run remote deploy` и упал уже на внешней публичной проверке. Это подтвердило, что права и bootstrap исправлены, а оставшаяся проблема относится к timing публичного health-check.
- Исправление по проверкам (2026-05-23): во workflow добавлены retry-циклы для `https://app.anyatobolova.ru/mini-api/health` и `https://app.anyatobolova.ru/`, чтобы dev-автодеплой не падал на коротком окне прогрева после успешной выкладки.

---

## Этап 28. Сквозная проверка bot + mini app + calendar + Telegram

Статус: `Не начат`

### Чек-лист текущего прогресса по этапу

- [ ] Проверить клиентский путь `бот -> mini app -> заявка -> решение тренера -> статус у клиента`.
- [ ] Проверить тренерский путь `mini app -> работа с заявками -> тренировки -> слоты`.
- [ ] Проверить уведомления, fallback в бот и прямую связь через Telegram.
- [ ] Проверить календарные сценарии: подтверждение, перенос, отмена, `.ics` у клиента.
- [ ] Проверить резерв слота до решения тренера на кейсе двух клиентов.
- [ ] Проверить mobile Telegram и desktop Telegram.

### Логи по этапу

- Цель этапа: убедиться, что mini app не существует отдельно от бота, а реально работает как часть общей системы записи.
- Подготовка к этапу (2026-05-24): в `apps/bot` добавлен безопасный опциональный вход в mini app через env-переменную `BOT_MINI_APP_URL`.
  - Если переменная не задана, production-поведение бота не меняется.
  - Если переменная задана у отдельного test bot, в `/start` и `/miniapp` появляется кнопка входа в mini app.
- Документация к этапу (2026-05-24): создан `docs/telegram-test-bot.md` с полным планом безопасной Telegram-проверки через отдельного test bot, без затрагивания production-бота.

---

## Этап 29. Soft launch mini app

Статус: `Не начат`

### Чек-лист текущего прогресса по этапу

- [ ] Определить ограниченный круг первых пользователей и сценарий включения mini app.
- [ ] Проверить работу mini app на VPS в живом Telegram-контуре.
- [ ] Собрать замечания по клиенту и тренеру уже не в локальном, а во внешнем окружении.
- [ ] Исправить критичные баги первого запуска.
- [ ] Подтвердить, что mini app можно безопасно использовать параллельно с ботом.

### Логи по этапу

- Цель этапа: мягко запустить mini app без резкого переключения пользователей и без риска для рабочего Telegram-бота.

---

## Этап 30. Production-подготовка и следующая итерация доработок

Статус: `Не начат`

### Чек-лист текущего прогресса по этапу

- [ ] Подготовить решение о том, когда и как mini app становится частью production-схемы.
- [ ] Согласовать, какие функции остаются в боте как резервные, а какие окончательно закрепляются за mini app.
- [ ] Сформировать backlog второй итерации после первой полной проверки.
- [ ] Описать регламент сопровождения: локальный старт, GitHub, deploy, rollback, проверка календаря, проверка уведомлений.
- [ ] Зафиксировать, что еще нужно сделать после первого полного запуска mini app вместе с ботом.

### Логи по этапу

- Цель этапа: не закончить работу на интерфейсе, а довести mini app до понятного процесса сопровождения и следующего цикла улучшений.

